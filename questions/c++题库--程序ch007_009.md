## ch007_009
``` c++
1)　　#include<iostream>
2)　　#include<iomanip>
3)　　using　namespace　std;
4)　　void　main(){
5)　　　　//建立小孩数组
6)　　　　const　int　numOfBoy　=　7;　　　　//小孩总数
7)　　　　int　m;　　　　　　　　//Josephus问题中的间隔m
8)　　　　int　boy[numOfBoy];　　　　　　　　　　//小孩数组
9)　　　　//给小孩编号
10)　　　　for　(int　i　=　0;　i<numOfBoy;　i++)　　　//从1开始编号
11)　　　　　boy[i]　=　i　+　1;
12)　　　　　//输入数小孩间隔
13)　　　　　cout　<<　"please　input　the　interval:　";
14)　　　　　cin　>>　m;
15)　　　　　//输出初始状态
16)　　　　for　(int　i　=　0;　i<numOfBoy;　i++)　　　　//顺序输出开始时的小孩编号
17)　　　　　　cout　<<　boy[i]　<<　"，";
18)　　　　　　cout　<<　endl;
19)　　　　　//小孩依次离开
20)　　　　　int　count　=　1;　　　　　　//离开小孩的个数
21)　　　　　int　i　=　numOfBoy　-　1;　　　　　//第一个小孩的前一个
22)　　　do{
23)　　　　　//在圈中数小孩
24)　　　　　int　j　=　0;
25)　　　　do{
26)　　　　　　　i　=　(i　+　1)　%　numOfBoy;　　　　　　　//数组中的下一个位置
27)　　　　　　　if　(boy[i]　!=　0)　　　　　　　　//数小孩，跳过不在圆圈中的元素
28)　　　　　　　j++;
29)　　　　　　}　while　(j　<　m);
30)　　　　　//数到的小孩离开
31)　　　　　cout　<<　boy[i]　<<　"，";　　　//输出离开的小孩之编号
32)　　　　　boy[i]　=　0;　　　　　　　　　　　　　　//标识该小孩已离开
33)　　　　　count++;　　　　　　　　　　　　　　　　　//离开小孩数加1
34)　　　　}　while　(count　<numOfBoy);//只剩一个小孩
35)　　　　//从数组中输出获胜小孩
36)　　　for　(int　i　=　0;　i<numOfBoy;　i++){
37)　　　　if　(boy[i]　!=　0)　{
38)　　　　cout　<<　"\nNo."　<<　boy[i]　<<　"获胜.\n";　　　//输出胜利者
39)　　　　break;
40)　　　　}
41)　　　}
42)　　
43)　　　system("pause");
44)　　}

```
### 题目23660
问题：程序第11行总共执行（）次。

A、7
B、8
C、9
D、10


**解析如下：**

------

解析：程序第11行位于一个for循环中，该循环的初始条件是`i = 0`，终止条件是`i < numOfBoy`，且每次循环`i`递增1。根据程序第6行的定义，`numOfBoy`的值为7，因此该循环会从0迭代到6，总共执行7次。

所以，第11行总共执行7次。

答案：A

```json
{"23660":"A"}
```

------

### 题目23661
问题：程序第10行中循环判断语句“i<numOfBoy”总共执行（）次。

A、10
B、9
C、7
D、8


**解析如下：**

------

解析：程序第10行的循环判断语句“i<numOfBoy”位于一个for循环中。for循环的初始条件是`i = 0`，终止条件是`i < numOfBoy`，且每次循环`i`递增1。`numOfBoy`的值为7。

在for循环中，循环判断语句会在每次循环开始时执行一次，并且在循环结束时再执行一次以确定是否继续循环。因此，循环判断语句的总执行次数为循环次数加1。

由于循环体执行7次，所以循环判断语句“i<numOfBoy”总共执行7 + 1 = 8次。

答案：D

```json
{"23661":"D"}
```

------

### 题目23662
问题：程序执行完第8行后，boy[7]的值为（）。

A、10
B、0
C、随机值
D、不存在boy[7]


**解析如下：**

------

解析：程序第8行定义了一个数组`boy[numOfBoy]`，其中`numOfBoy`的值为7。因此，数组`boy`的大小为7，即`boy[0]`到`boy[6]`。

在C++中，数组的索引从0开始，所以`boy[7]`是数组`boy`的一个越界访问。访问越界的数组元素是未定义行为，可能会得到随机值，也可能会引发运行时错误。

但在题目中，只问执行完第8行后的情况，此时数组刚刚被定义，还没有进行任何赋值操作，所以`boy[7]`并不存在。

答案：D

```json
{"23662":"D"}
```

------

### 题目23663
问题：程序执行完第8行后，boy[0]的值为（）。

A、0
B、1
C、随机值
D、不存在boy[0]


**解析如下：**

------

解析：程序第8行定义了一个数组`boy[numOfBoy]`，其中`numOfBoy`的值为7。因此，数组`boy`的大小为7，即`boy[0]`到`boy[6]`。

在C++中，定义数组时，如果没有显式初始化，数组元素的值是未定义的。但在程序的第10行到第11行，有一个循环对数组`boy`进行了初始化：

```cpp
for (int i = 0; i < numOfBoy; i++)
    boy[i] = i + 1;
```

这个循环将`boy[0]`到`boy[6]`分别初始化为1到7。因此，执行完第8行后，虽然`boy[0]`的值还未被显式赋值，但在执行完第11行后，`boy[0]`的值会被赋为1。

题目问的是执行完第8行后的情况，严格来说，此时`boy[0]`的值是未定义的。但考虑到后续的初始化操作，我们可以推断出最终`boy[0]`的值将为1。

答案：B

```json
{"23663":"B"}
```

------

### 题目23664
问题：程序执行完第7行后，变量m的值为（）。

A、0
B、随机值
C、FFFFf
D、因编译器不同而不同


**解析如下：**

------

解析：程序第7行定义了一个变量`m`，并声明其类型为`int`：

```cpp
int m; // Josephus问题中的间隔m
```

在C++中，局部变量（如`m`）如果没有显式初始化，其值是未定义的（undefined）。这意味着`m`的值可以是任何随机值，具体取决于程序运行时的内存状态。

因此，执行完第7行后，变量`m`的值是一个随机值。

答案：B

```json
{"23664":"B"}
```

------

### 题目23665
问题：程序在执行第44行之前，此时内存中有（）个变量。

A、3
B、4
C、5
D、6


**解析如下：**

------

解析：我们需要分析程序中定义的变量数量。以下是程序中定义的变量：

1. `numOfBoy`（第6行）
2. `m`（第7行）
3. `boy`（第8行，这是一个数组，但整体算一个变量）
4. `count`（第20行）
5. `i`（第21行）
6. `j`（第24行）

这些变量都是在`main`函数中定义的，且在执行到第44行之前都还未超出其作用域。

因此，程序在执行第44行之前，内存中一共有6个变量。

答案：D

```json
{"23665":"D"}
```

------

### 题目23666
问题：若用户在执行第14行时输入5并回车，则程序第38行的输出结果为（）。

A、　　No.6获胜
B、　　No.2获胜
C、　　No.1获胜
D、　　No.4获胜


**解析如下：**

------

解析：程序实现了约瑟夫环问题。假设用户在第14行输入5，表示每隔5个人淘汰一个。

我们可以手动模拟这个过程：

1. 初始状态：`[1, 2, 3, 4, 5, 6, 7]`
2. 从第7个人开始数（索引为6），数到第5个人（索引为4），淘汰`5`：`[1, 2, 3, 4, 0, 6, 7]`
3. 从第5个人（索引为4）开始数，数到第5个人（索引为1），淘汰`2`：`[1, 0, 3, 4, 0, 6, 7]`
4. 从第2个人（索引为1）开始数，数到第5个人（索引为7），淘汰`7`：`[1, 0, 3, 4, 0, 6, 0]`
5. 从第7个人（索引为6）开始数，数到第5个人（索引为3），淘汰`4`：`[1, 0, 3, 0, 0, 6, 0]`
6. 从第4个人（索引为3）开始数，数到第5个人（索引为6），淘汰`6`：`[1, 0, 3, 0, 0, 0, 0]`
7. 从第6个人（索引为5）开始数，数到第5个人（索引为2），淘汰`3`：`[1, 0, 0, 0, 0, 0, 0]`

最后剩下的是`1`。

因此，程序第38行的输出结果为`No.1获胜`。

答案：C

```json
{"23666":"C"}
```

------

### 题目23667
问题：若用户在执行第14行时输入5并回车，则程序外层while循环执行完第1次后，boy[4]的值为（）。

A、0
B、1
C、4
D、5


**解析如下：**

------

解析：程序实现了约瑟夫环问题。假设用户在第14行输入5，表示每隔5个人淘汰一个。

我们手动模拟外层while循环执行第1次的过程：

1. 初始状态：`[1, 2, 3, 4, 5, 6, 7]`
2. 从第7个人（索引为6）开始数，数到第5个人（索引为4），淘汰`5`。

具体步骤如下：
- 初始`i = 6`（第21行）
- 进入外层while循环，开始内层do-while循环：
  - `j = 0`，`i = (6 + 1) % 7 = 0`，`boy[0] = 1`，`j = 1`
  - `j = 1`，`i = (0 + 1) % 7 = 1`，`boy[1] = 2`，`j = 2`
  - `j = 2`，`i = (1 + 1) % 7 = 2`，`boy[2] = 3`，`j = 3`
  - `j = 3`，`i = (2 + 1) % 7 = 3`，`boy[3] = 4`，`j = 4`
  - `j = 4`，`i = (3 + 1) % 7 = 4`，`boy[4] = 5`，`j = 5`（达到间隔数5）

此时，数到的小孩（索引为4，即`boy[4]`）离开，`boy[4]`被设置为0。

因此，外层while循环执行完第1次后，`boy[4]`的值为0。

答案：A

```json
{"23667":"A"}
```

------

### 题目23668
问题：若用户在执行第14行时输入5并回车，则程序在第1次执行完while循环后，boy[0]的值为（）。

A、0
B、1
C、2
D、3


**解析如下：**

------

解析：程序实现了约瑟夫环问题。假设用户在第14行输入5，表示每隔5个人淘汰一个。

我们手动模拟第1次执行完while循环的过程：

1. 初始状态：`[1, 2, 3, 4, 5, 6, 7]`
2. 从第7个人（索引为6）开始数，数到第5个人（索引为4），淘汰`5`。

具体步骤如下：
- 初始`i = 6`（第21行）
- 进入外层while循环，开始内层do-while循环：
  - `j = 0`，`i = (6 + 1) % 7 = 0`，`boy[0] = 1`，`j = 1`
  - `j = 1`，`i = (0 + 1) % 7 = 1`，`boy[1] = 2`，`j = 2`
  - `j = 2`，`i = (1 + 1) % 7 = 2`，`boy[2] = 3`，`j = 3`
  - `j = 3`，`i = (2 + 1) % 7 = 3`，`boy[3] = 4`，`j = 4`
  - `j = 4`，`i = (3 + 1) % 7 = 4`，`boy[4] = 5`，`j = 5`（达到间隔数5）

此时，数到的小孩（索引为4，即`boy[4]`）离开，`boy[4]`被设置为0。

在这个过程中，`boy[0]`的值始终为1，没有被改变。

因此，第1次执行完while循环后，`boy[0]`的值为1。

答案：B

```json
{"23668":"B"}
```

------

### 题目23669
问题：若用户在执行第14行时候输入5并回车，则程序第1次执行第31行时，boy[i]的值为（）。

A、0
B、1
C、4
D、5


**解析如下：**

------

解析：程序实现了约瑟夫环问题。假设用户在第14行输入5，表示每隔5个人淘汰一个。

我们手动模拟第1次执行到第31行的过程：

1. 初始状态：`[1, 2, 3, 4, 5, 6, 7]`
2. 从第7个人（索引为6）开始数，数到第5个人（索引为4），淘汰`5`。

具体步骤如下：
- 初始`i = 6`（第21行）
- 进入外层while循环，开始内层do-while循环：
  - `j = 0`，`i = (6 + 1) % 7 = 0`，`boy[0] = 1`，`j = 1`
  - `j = 1`，`i = (0 + 1) % 7 = 1`，`boy[1] = 2`，`j = 2`
  - `j = 2`，`i = (1 + 1) % 7 = 2`，`boy[2] = 3`，`j = 3`
  - `j = 3`，`i = (2 + 1) % 7 = 3`，`boy[3] = 4`，`j = 4`
  - `j = 4`，`i = (3 + 1) % 7 = 4`，`boy[4] = 5`，`j = 5`（达到间隔数5）

此时，数到的小孩（索引为4，即`boy[4]`）离开，执行第31行时，`boy[i]`的值为`boy[4]`，即5。

因此，第1次执行第31行时，`boy[i]`的值为5。

答案：D

```json
{"23669":"D"}
```

------

### 题目23670
问题：程序执行完第7行后，变量m存储在内存空间的（）。

A、全局变量区
B、栈区
C、代码区
D、堆区


**解析如下：**

------

解析：程序第7行定义了一个变量`m`：

```cpp
int m; // Josephus问题中的间隔m
```

在C++中，变量`m`是在`main`函数内部定义的，属于局部变量。局部变量存储在栈区（stack）。

- 全局变量区：用于存储全局变量和静态变量。
- 栈区：用于存储局部变量和函数的参数。
- 代码区：用于存储程序的代码。
- 堆区：用于动态分配的内存。

因此，变量`m`存储在内存的栈区。

答案：B

```json
{"23670":"B"}
```

------

### 题目23671
问题：程序第31行中cout语句的返回值为（）。

A、cout对象
B、cout后的值
C、光标移动
D、以上都可以


**解析如下：**

------

解析：程序第31行中的`cout`语句如下：

```cpp
cout << boy[i] << ",";
```

在C++中，`cout`是标准输出流对象，它是`ostream`类的实例。`cout`的作用是将数据输出到标准输出设备，通常是终端或屏幕。`cout`语句的返回值是`cout`对象本身，这允许链式调用，即可以在同一行中连续输出多个值。

因此，程序第31行中`cout`语句的返回值是`cout`对象。

答案：A

```json
{"23671":"A"}
```

------

### 题目23672
问题：程序第28行的语义为（）。

A、首先将j的值存放在一个副本空间内，然后将j的值加1
B、首先将j的值加1，然后再将所得值存入一个副本空间内
C、将1加入j内
D、将j的值取出，然后加1


**解析如下：**

------

解析：程序第28行是如下语句：

```cpp
j++;
```

在C++中，`j++`是一个后缀自增运算符，它的语义是首先返回`j`的当前值，然后将`j`的值加1。这是因为在后缀自增运算中，表达式`j++`的值是`j`的原始值。

因此，程序第28行的语义是首先将`j`的值存放在一个副本空间内，然后将`j`的值加1。

答案：A

```json
{"23672":"A"}
```

------

### 题目23673
问题：程序第27中的if语句，如果其判定非真，则程序将执行（）。

A、第28行
B、第26行
C、第27行
D、语句“j<m”


**解析如下：**

------

解析：程序第27行的`if`语句如下：

```cpp
if (boy[i] != 0)
```

这是一个简单的条件判断。如果条件为真（即`boy[i]`不等于0），则执行`if`语句块内的代码。如果条件为假（即`boy[i]`等于0），则跳过`if`语句块内的代码，继续执行`if`语句后面的代码。

在`if`语句的判定非真（即`boy[i]`等于0）的情况下，程序将执行`if`语句块后面的代码，即第28行。

答案：A

```json
{"23673":"A"}
```

------

### 题目23674
问题：程序第26行中，最先执行的表达式是（）。

A、i=(i+1)%　numOfBoy；
B、i=(i+1)
C、i+1
D、(i+1)%　numOfBoy


**解析如下：**

------

解析：程序第26行的代码如下：

```cpp
i = (i + 1) % numOfBoy;
```

这是一个复合赋值表达式。在C++中，表达式的执行顺序是从右到左，即首先计算括号内的加法`i + 1`，然后计算取模运算`% numOfBoy`，最后将结果赋值给变量`i`。

因此，程序第26行中最先执行的表达式是`i + 1`。

答案：C

```json
{"23674":"C"}
```

------

### 题目23675
问题：程序第26行中，优先级最高的操作符是（）。

A、A　=
B、()
C、+
D、%


**解析如下：**

------

解析：程序第26行的代码如下：

```cpp
i = (i + 1) % numOfBoy;
```

在C++中，操作符的优先级决定了表达式中操作的顺序。括号`()`具有最高的优先级，因此首先执行括号内的加法`i + 1`。然后是取模运算符`%`，最后是赋值运算符`=`。

在这个表达式中，优先级最高的操作符是括号`()`。

答案：B

```json
{"23675":"B"}
```

------

