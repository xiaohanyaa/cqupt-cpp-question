## ch005_005
``` c++
1）int n = 10;
2）void f(){
3）  n++;
4）  cout << "n=" << n << endl;
5）  static int n = 2;
6）  cout << "n:" << n << endl;
7）  n++;  
8）  cout << "n:" << n << endl;
9）}
10）void main(){
11）  n++;
12）  cout << "n=" << n << endl;
13）  int n = 0;
14)    cout << "n=" << n << endl;
15)    cout << "f" << endl;
16)    f();
17)    cout << "f" << endl;
18)    cout << "n=" << n << endl;
19)    f();
20)    cout << "n=" << ::n << endl;
21)  }

```
### 题目23100
问题：43.  当执行完第14行代码，12行和14行的n分别在哪个存储区：
A. 栈区，栈区
B. 栈区，全局数据区
C. 全局数据区，全局数据区
D. 全局数据区，栈区


**解析如下：**

------

解析：
- 第12行的`n`是全局变量，存储在全局数据区。
- 第14行的`n`是局部变量，存储在栈区。

因此，正确答案是D。

{'23100':'D'}

------

### 题目23101
问题：44.  当执行完第14行代码，12行和14行的n的值分别为：
A. 1，0
B. 1，10
C. 11，0
D. 11，11


**解析如下：**

------

解析：
- 第12行的`n`是全局变量，初始值为10，在第11行执行`n++`后，其值变为11。
- 第14行的`n`是局部变量，初始值为0。

因此，正确答案是C。

{'23101':'C'}

------

### 题目23102
问题：45.  如果让11行的代码和13行的代码调换位置，则12行和14行的n值为：
A. 0,1
B. 10,11
C. 0,11
D. 10,1


**解析如下：**

------

解析：
优先使用近距离变量。int n = 0, 输出, 再自增，输出1


{'23102':'A'}

------

### 题目23103
问题：46.  当执行完16行的代码时，全局数据区的变量数为：
A. 0
B. 1
C. 2
D. 3


**解析如下：**

------

解析：
- 全局数据区的变量包括全局变量和静态变量。
- 全局变量`n`在第1行定义，初始值为10。
- 静态变量`n`在第5行定义，初始值为2。

因此，全局数据区的变量数为2。

{'23103':'C'}

------

### 题目23104
问题：47.  当执行完16行的代码时，栈区的变量数为：
A. 0
B. 1
C. 2
D. 3


**解析如下：**

------

解析：
- 栈区的变量包括局部变量。
- 在`main`函数中，第13行定义了一个局部变量`n`，初始值为0。
- 在`f`函数中，没有定义局部变量。

因此，栈区的变量数为1。

{'23104':'B'}

------

### 题目23105
问题：48.  当执行完16行的代码时，全局变量n为：
A. 10
B. 11
C. 12
D. 13


**解析如下：**

------

解析：
优先使用近距离变量，没有的话再往大一点的范围寻找

{'23105':'C'}

------

### 题目23106
问题：49.  当执行完16行的代码时，静态局部变量n为：
A. 0
B. 1
C. 2
D. 3


**解析如下：**

------

解析：
- 静态局部变量`n`在第5行定义，初始值为2。
- 在`f`函数中，第7行执行`n++`，使得静态局部变量`n`的值变为3。

因此，执行完16行的代码时，静态局部变量`n`的值为3。

{'23106':'D'}

------

### 题目23107
问题：50.  将第3行和第5行调换位置，当执行完16行的代码时，全局变量n为：
A. 10
B. 11
C. 12
D. 13


**解析如下：**

------

解析：
- 将第3行和第5行调换位置后，第3行定义静态局部变量`n`，初始值为2。
- 第5行执行`n++`，但由于`n`是静态局部变量，不会影响全局变量`n`。
- 全局变量`n`在第1行定义，初始值为10。
- 在`main`函数中，第11行执行`n++`，使得全局变量`n`的值变为11。

因此，执行完16行的代码时，全局变量`n`的值为11。

{'23107':'B'}

------

### 题目23108
问题：51.  将第3行和第5行调换位置，当执行完16行的代码时，静态局部变量n为：
A. 1
B. 2
C. 3
D. 4


**解析如下：**

------

解析：
- 将第3行和第5行调换位置后，第3行定义静态局部变量`n`，初始值为2。
- 第5行执行`n++`，使得静态局部变量`n`的值变为3。
- 第7行执行`n++`，使得静态局部变量`n`的值变为4。

{'23108':'D'}

------

### 题目23109
问题：52.  当删除第5行代码，当执行完16行的代码时，则8行n值为：
A. 11
B. 1
C. 13
D. 2


**解析如下：**

------

解析：
- 删除第5行代码后，`f`函数中没有定义静态局部变量`n`。
- 第3行的`n++`操作会修改全局变量`n`，因为`f`函数中没有局部变量`n`。
- 全局变量`n`在第1行定义，初始值为10。
- 在`main`函数中，第11行执行`n++`，使得全局变量`n`的值变为11。
- 在`f`函数中，第3行执行`n++`，使得全局变量`n`的值变为12。
- 第7行执行`n++`，使得全局变量`n`的值变为13。

因此，执行完16行的代码时，第8行的`n`值为13。

{'23109':'C'}

------

### 题目23110
问题：53.  当删除第5行代码中static后，当执行完16行的代码时，则8行n值为：
A. 2
B. 1
C. 13
D. 3


**解析如下：**

------

解析：
- 删除第5行代码中的`static`后，`n`成为局部变量。
- 第3行的`n++`操作会修改局部变量`n`，初始值为2。
- 第7行的`n++`操作会继续修改局部变量`n`，使其值变为3。

因此，执行完16行的代码时，第8行的`n`值为3。

{'23110':'D'}

------

### 题目23111
问题：54.  当将第5行代码变为static int n，当执行完16行的代码时，则8行n值为：
A. 2
B. 1
C. 0
D. 3


**解析如下：**

------

解析：
- 将第5行代码变为`static int n`后，`n`成为静态局部变量。
- 第7行的`n++`操作会继续修改静态局部变量`n`，使其值变为1。

{'23111':'B'}

------

### 题目23112
问题：55.  第18行的n值存储在哪个区：
A. 全局数据区
B. 栈区
C. 代码区
D. 堆区


**解析如下：**

------

解析：
- 第18行的`n`是局部变量，存储在栈区。

因此，正确答案是B。

{'23112':'B'}

------

### 题目23113
问题：56.  第20行的n值，如果将“::”去掉后n存储在哪个区：
A. 全局数据区
B. 栈区
C. 代码区
D. 堆区


**解析如下：**

------

解析：
- 第20行的`n`是局部变量，存储在栈区。
- 如果将“::”去掉，`n`仍然是局部变量，存储在栈区。

因此，正确答案是B。

{'23113':'B'}

------

### 题目23114
问题：57.  当执行完19行的代码时，静态局部变量n为：
A. 4
B. 5
C. 6
D. 3


**解析如下：**

------

解析：
- 静态局部变量`n`在第5行定义，初始值为2。
- 在`f`函数中，第7行执行`n++`，使得静态局部变量`n`的值变为3。
- 执行完16行的代码后，静态局部变量`n`的值为3。
- 再次调用`f`函数（第19行），第7行执行`n++`，使得静态局部变量`n`的值变为4。

因此，执行完19行的代码时，静态局部变量`n`的值为4。

{'23114':'A'}

------

### 题目23115
问题：58.  当执行完19行的代码时，全局变量n为：
A. 11
B. 12
C. 13
D. 10


**解析如下：**

------

解析：
- 全局变量`n`在第1行定义，初始值为10。
- 在`main`函数中，第11行执行`n++`，使得全局变量`n`的值变为11。
- 在`f`函数中，第3行执行`n++`，使得全局变量`n`的值变为12。
- 执行完16行的代码后，全局变量`n`的值为12。
- 再次调用`f`函数（第19行），第3行执行`n++`，使得全局变量`n`的值变为13。

因此，执行完19行的代码时，全局变量`n`的值为13。

{'23115':'C'}

------

### 题目23116
问题：59.  当删除第5行代码中static后，当执行完19行的代码时，局部变量n为：
A. 0
B. 1
C. 2
D. 3


**解析如下：**

------

解析：
13行的局部变量n没有被修改，所以main函数中局部变量n = 0

{'23116':'A'}

------

### 题目23117
问题：60.  将第5行代码变为static int n，当执行完19行的代码时，第8行n为：
A. 0
B. 1
C. 2
D. 3


**解析如下：**

------

解析：
- 将第5行代码变为`static int n`后，`n`成为静态局部变量。
- 在`f`函数中，第3行执行`n++`，使得静态局部变量`n`的值变为1。
- 执行完16行的代码后，静态局部变量`n`的值为1。
- 再次调用`f`函数（第19行），第3行执行`n++`，使得静态局部变量`n`的值变为2。

因此，执行完19行的代码时，第8行的`n`值为2。

{'23117':'C'}

------

