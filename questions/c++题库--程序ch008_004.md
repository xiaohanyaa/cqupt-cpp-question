## ch008_004
``` c++
1)  #include<iostream>
2)  using namespace std;
3)  int maximum(int[][4], int, int);
4)  
5)  void main(){
6)   int sg[3][4] = {
7)     { 68, 77, 73, 86 },
8)     { 87, 96, 78, 89 },
9)     { 90, 70, 81, 86 }
10)    };
11)   cout << "the max grade is " << maximum(sg, 3, 4) << endl;
12)   system("pause");
13)  }
14)  
15)  int maximum(int grade[][4], int pupils, int tests)
16)  {
17)    int max = 0;
18)    for (int i = 0; i<pupils; i++){
19)    for (int j = 0; j<tests; j++){
20)    if (grade[i][j]>max)
21)    max = grade[i][j];
22)    }
23)   }
24)   return max;
25)  }

```
### 题目23760
问题：程序第3行的作用是：

A、函数声明
B、函数定义
C、函数重载
D、函数调用


**解析如下：**

------

解析：
第3行的代码 `int maximum(int[][4], int, int);` 是一个函数声明。它告诉编译器有一个名为 `maximum` 的函数，该函数接受一个二维数组和两个整数作为参数，并返回一个整数。函数声明的作用是让编译器知道这个函数的存在，以便在后续的代码中调用它。

{'23760':'A'}

------

### 题目23761
问题：程序执行到第6行时，sg[0][2]的值是：

A、73
B、0
C、68
D、随机值


**解析如下：**

------

解析：
题库提示：程序为sg数组分配了内存空间，但sg数组还未初始化，sg[0][2]也没有被初始化，它的值是随机值。

{'23761':'D'}

------

### 题目23762
问题：程序执行到第7行时，sg[1][3]的值是：

A、89
B、78
C、96
D、随机值


**解析如下：**

------

解析：
题库提示：当程序执行到第7行时，程序为sg数组的第1行元素初始化，没有对第2,3行初始化。sg[1][3]是第2行的元素，没有被初始化，它的值为随机值。
{'23762':'D'}

------

### 题目23763
问题：程序执行到第8行时，sg[1][2]的值是：

A、78
B、0
C、96
D、随机值

**解析如下：**

------

解析：
程序执行到第8行时，二维数组 `sg` 已经被初始化。根据初始化列表，`sg[1][2]` 的值是 `78`。初始化列表中的数据按照行优先的顺序填充到数组中，因此 `sg[1][2]` 对应的是第二行第三列的值。

{'23763':'A'}

------

### 题目23764
问题：逐语句单步调试程序，当程序执行到第11行时，紧接着执行哪一行：

A、第13行
B、第14行
C、第15行
D、第16行


**解析如下：**

------

解析：
题库提示： 当执行到第11行时，main函数会调用maximum()函数，转到第16行执行。

{'23764':'D'}

------

### 题目23765
问题：程序第11行maximum(sg,3,4)中实参sg的含义是：

A、数组sg的值
B、数组sg的行数
C、数组sg的列数
D、数组sg的首地址


**解析如下：**

------

解析：
在C++中，数组名作为函数参数传递时，传递的是数组的首地址。因此，`maximum(sg, 3, 4)` 中的实参 `sg` 的含义是数组 `sg` 的首地址。

{'23765':'D'}

------

### 题目23766
问题：当程序运行到第16行时，第15行的变量pupils的值是：

A、0
B、1
C、2
D、3


**解析如下：**

------

解析：
当程序运行到第16行时，`maximum` 函数已经被调用，并且传递了参数 `sg`、`3` 和 `4`。在 `maximum` 函数中，参数 `pupils` 的值是 `3`，因为这是在调用 `maximum` 函数时传递的第二个参数。

{'23766':'D'}

------

### 题目23767
问题：当程序运行到第16行时，第15行的变量tests的值是：

A、1
B、2
C、3
D、4


**解析如下：**

------

解析：
当程序运行到第16行时，`maximum` 函数已经被调用，并且传递了参数 `sg`、`3` 和 `4`。在 `maximum` 函数中，参数 `tests` 的值是 `4`，因为这是在调用 `maximum` 函数时传递的第三个参数。

{'23767':'D'}

------

### 题目23768
问题：第18行的for循环共执行几次：

A、1次
B、2次
C、3次
D、4次


**解析如下：**

------

解析：
第18行的 `for` 循环的执行次数取决于 `pupils` 的值。在 `maximum` 函数中，`pupils` 的值是 `3`，因此 `for` 循环会执行 `3` 次。

{'23768':'C'}

------

### 题目23769
问题：第19行的for循环共执行几次：

A、1次
B、2次
C、3次
D、4次


**解析如下：**

------

解析：
第19行的 `for` 循环的执行次数取决于 `tests` 的值。在 `maximum` 函数中，`tests` 的值是 `4`，因此 `for` 循环会执行 `4` 次。

{'23769':'D'}

------

### 题目23770
问题：在执行for循环时，若变量i的值为0，变量j的值为2，则执行完第21行后，变量max的值是：

A、68
B、77
C、73
D、86


**解析如下：**

------

解析：
题库提示：当i=0，j=0时，max=68，，当i=0，j=1时，max=77，执行第3次时，i=0，j=2，73比max的值77小，max的值不变仍为77，因此答案选B。

{'23770':'B'}

------

### 题目23771
问题：当程序执行for循环时，若变量i的值为2，变量j的值为2，则执行完第21行后，变量max的值是：

A、96
B、78
C、89
D、90


**解析如下：**

------

解析：
题库提示：由sg[1][1]=96可知, for循环执行到变量i的值为1，变量j的值为1时，变量max的值被修改为96。96是数组中的最大值，i的值为2，j的值为2时没有改变max的值，所以选A。

{'23771':'A'}

------

### 题目23772
问题：程序执行完第24行后，maximum()函数的返回值是：

A、77
B、90
C、96
D、81


**解析如下：**

------

解析：
程序执行完第24行后，`maximum()` 函数的返回值是 `96`。在 `maximum` 函数中，`max` 变量会被更新为数组 `grade` 中的最大值。根据初始化列表，数组 `grade` 中的最大值是 `96`。

{'23772':'C'}

------

### 题目23773
问题：当程序执行到第25行时，变量i的值是：

A、3
B、2
C、1
D、变量i不存在


**解析如下：**

------

解析：
当程序执行到第25行时，`for` 循环已经结束。变量i的生命周期在for循环结束时就已经结束。程序运行到第19行时，i的内存空间已经被释放。

{'23773':'D'}

------

### 题目23774
问题：当程序执行到第25行时，变量j的值是：

A、4
B、3
C、2
D、变量j不存在


**解析如下：**

------

解析：
跟上同理。

{'23774':'D'}

------

### 题目23775
问题：当程序执行到第25行时，变量max的值是：

A、96
B、70
C、81
D、变量max不存在


**解析如下：**

------

解析：
当程序执行到第25行时，`maximum` 函数已经完成了对数组 `grade` 中所有元素的遍历，并找到了其中的最大值。根据初始化列表，数组 `grade` 中的最大值是 `96`。因此，当程序执行到第25行时，变量 `max` 的值是 `96`。

{'23775':'A'}

------

### 题目23776
问题：当程序执行完第25行之后，max的值是：

A、96
B、70
C、81
D、变量max不存在


**解析如下：**

------

解析：
题库提示：变量max的生命周期，当maximum()函数执行完之后，其内部定义的局部变量max的内存空间会被收回。

{'23776':'D'}

------

### 题目23777
问题：程序执行完第11行之后，屏幕上显示：

A、the max grade is maximum(sg,3,4)
B、the max grade is 68
C、the max grade is 96
D、以上结果都不对


**解析如下：**

------

解析：
程序执行完第11行之后，屏幕上会显示 `the max grade is 96`。因为在第11行，程序调用了 `maximum` 函数，并将其返回值（即数组 `sg` 中的最大值 `96`）输出到屏幕上。

{'23777':'C'}

------

