## ch008_007
``` c++
1)  #include<iostream>
2)  using namespace std;
3)  void strAdd(char* dest, char* source);
4)  void strCopy(char* dest, char* source);
5)  void main(){
6)    //字符串赋值
7)    char a[20] = "copy string.";
8)    char b[20] = "";
9)    strCopy(b, a);
10)   cout << b << endl;
11)   //字符串加法
12)   char c[30] = "computer ";
13)   strAdd(c, a);
14)   system("pause");
15)  }
16)  
17)  void strCopy(char* dest, char* source){
18)   while (*dest++ = *source++);
19)  }
20)  
21)  void strCopy1(char* dest, char* source) {
22)    while (*dest = *source){
23)    dest++;
24)    source++;
25)   }
26)  }
27)  
28)  void strAdd(char* dest, char* source){
29)    while (*dest){//移到结束标志
30)    dest++;
31)  }
32)  while (*source){
33)    *dest = *source;//复制当前字符
34)    dest++;
35)    source++;
36)   };
37)   *dest = '\\0';//设置结束标志
38)  }

```
### 题目23807
问题：程序第3行为（）。

A、函数声明
B、函数调用
C、函数定义
D、函数实现


**解析如下：**

------

解析：
程序第3行是一个函数声明，它告诉编译器有一个名为 `strAdd` 的函数，该函数接受两个 `char*` 类型的参数。函数声明的作用是让编译器知道这个函数的存在，以便在后续代码中调用它。

{'23807':'A'}

------

### 题目23808
问题：程序第3行中* dest前面的char的含义是：

A、指针的类型是字符型
B、指针指向的变量为字符型
C、指针的地址为字符型
D、不能确定


**解析如下：**

------

解析：
在程序第3行中，`char* dest` 表示 `dest` 是一个指向字符型数据的指针。`char` 在这里的含义是指针 `dest` 所指向的变量的类型是字符型。

{'23808':'B'}

------

### 题目23809
问题：程序第7行数组a的实际大小是：

A、0B
B、1B
C、2B
D、20B


**解析如下：**

------

解析：
程序第7行中，`char a[20] = "copy string.";` 定义了一个大小为20的字符数组 `a`。数组的大小是20个字节（B），因为每个字符占用1个字节。

{'23809':'D'}

------

### 题目23810
问题：第8行中，变量b的类型是：

A、全局变量
B、局部变量
C、全局静态变量
D、局部静态变量


**解析如下：**

------

解析：
在程序第8行中，`char b[20] = "";` 定义了一个字符数组 `b`，它是在 `main` 函数内部定义的。因此，`b` 是一个局部变量，它的作用域仅限于 `main` 函数内部。

{'23810':'B'}

------

### 题目23811
问题：如果将第9行语句改为strCopy1(b,a);，则以下说法正确的是：

A、输出结果与之前一样
B、输出结果与之前不一样
C、程序报错
D、A、B都正确


**解析如下：**

------

解析：
在程序中，`strCopy` 和 `strCopy1` 都是用于将 `source` 字符串复制到 `dest` 字符串的函数。虽然它们的实现方式略有不同，但功能是相同的。因此，将第9行语句改为 `strCopy1(b, a);` 后，输出结果与之前是一样的。
但是，函数strCopy1在使用之前没有进行声明，所以会报错。

{'23811':'C'}

------

### 题目23812
问题：在程序第9行表达式中的b表示：

A、数组b的元素个数
B、数组b的第一个元素
C、数组b的首地址
D、整个数组b


**解析如下：**

------

解析：
在程序第9行中，`strCopy(b, a);` 中的 `b` 表示数组 `b` 的首地址。在C++中，数组名在大多数情况下会被解释为指向数组第一个元素的指针。

{'23812':'C'}

------

### 题目23813
问题：关于函数strCopy和函数strCopy1，以下说法正确的是：

A、函数strCopy和函数strCopy1的功能相同
B、函数strCopy和函数strCopy1的功能不相同
C、函数strCopy和函数strCopy1的返回类型不同
D、函数strCopy和函数strCopy1的都返回指向字符类型的指针


**解析如下：**

------

解析：
函数 `strCopy` 和 `strCopy1` 都是用于将 `source` 字符串复制到 `dest` 字符串的函数。它们的实现方式略有不同，但功能是相同的，都是将一个字符串的内容复制到另一个字符串中。

{'23813':'A'}

------

### 题目23814
问题：执行完第13行后，若执行cout<<c<<endl;，则输出的是：

A、computer
B、computer copy string.
C、copy string. computer
D、copy string


**解析如下：**

------

解析：
在程序第13行中，`strAdd(c, a);` 将字符串 `a` 的内容追加到字符串 `c` 的末尾。执行完第13行后，字符串 `c` 的内容变为 `"computer copy string."`。因此，执行 `cout << c << endl;` 时，输出的是 `"computer copy string."`。

{'23814':'B'}

------

### 题目23815
问题：程序第14行中system("pause")的作用是：

A、换行
B、来暂停黑窗口
C、设置字段的宽度
D、程序结束运行


**解析如下：**

------

解析：
在程序第14行中，`system("pause")` 的作用是暂停程序的执行，等待用户按下任意键继续。这通常用于在控制台应用程序中暂停黑窗口，以便用户可以看到输出结果。

{'23815':'B'}

------

### 题目23816
问题：程序第17行定义的strCopy（）函数返回值的类型是：

A、浮点型
B、字符型
C、整型
D、空类型


**解析如下：**

------

解析：
在程序第17行中，`void strCopy(char* dest, char* source)` 定义了一个名为 `strCopy` 的函数，其返回类型是 `void`，表示该函数不返回任何值。

{'23816':'D'}

------

### 题目23817
问题：第18行中while (*dest++ = *source++)下面哪个操作符优先级最低（）

A、后自增++
B、间接引用*
C、赋值=
D、其他


**解析如下：**

------

解析：
在第18行中，`while (*dest++ = *source++);` 表达式中，操作符的优先级从高到低依次是：
1. 间接引用 `*`
2. 后自增 `++`
3. 赋值 `=`

因此，赋值操作符 `=` 的优先级最低。

{'23817':'C'}

------

### 题目23818
问题：第18行中while (*dest++ = *source++)终止条件是（）

A、*source！=*dest
B、source！=dest
C、dest =’\\0’
D、*dest=’\\0’


**解析如下：**

------

解析：
在第18行中，`while (*dest++ = *source++);` 的终止条件是当 `*source` 为 `'\0'` 时，因为 `'\0'` 是字符串的结束标志。当 `*source` 为 `'\0'` 时，赋值操作会将 `'\0'` 赋值给 `*dest`，此时条件为假，循环终止。

{'23818':'D'}

------

### 题目23819
问题：程序第28行为（）。

A、函数声明
B、函数调用
C、函数定义
D、函数实现


**解析如下：**

------

解析：
程序第28行是 `void strAdd(char* dest, char* source)`，这是一个函数定义，它不仅声明了函数 `strAdd` 的名称和参数，还提供了函数的具体实现。

{'23819':'C'}

------

### 题目23820
问题：如果删去第29、30、31行，则执行完第13行后字符数组c中的字符串是：

A、copy string.
B、computer
C、copy string. computer
D、computer copy string.


**解析如下：**

------

解析：
如果删去第29、30、31行，`strAdd` 函数将不再移动到 `dest` 字符串的末尾，而是直接从 `dest` 的第一个字符开始复制 `source` 的内容。因此，执行完第13行后，字符数组 `c` 中的字符串将是 `"copy string."`，因为 `source` 的内容会覆盖 `dest` 的内容。

{'23820':'A'}

------

### 题目23821
问题：32行while (*source)的结束条件是：

A、*source为’\\0’
B、*source为最后一个字母
C、*source为第一个元素
D、*source为第二个元素


**解析如下：**

------

解析：
在第32行中，`while (*source)` 的结束条件是当 `*source` 为 `'\0'` 时，因为 `'\0'` 是字符串的结束标志。当 `*source` 为 `'\0'` 时，条件为假，循环终止。

{'23821':'A'}

------

