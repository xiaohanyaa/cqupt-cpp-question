## ch006_001
``` c++
1)  //**********************
2)  //** ch06_01（共26题）**
3)  //**********************
4)  //file1.cpp
5)  
6)  #include <iostream>
7)  #include "file2.h"
8)  using namespace std;
9)  
10)  void main(){
11)    fn();
12)    //staticFn();
13)    cout << "全局变量n = " << n << endl;
14)    //cout<< "静态全局变量s = "<< s <<endl;
15)    system("pause");
16)  }
17)  //file2.cpp
18)  
19)  #include "file2.h"
20)  #include <iostream>
21)  using namespace std;
22)  
23)  static void staticFn();
24)  
25)  int n;
26)  static int s;
27)  
28)  void fn(){
29)    staticFn();
30)    cout << "this is fn()\n";
31)    n = 1;
32)    s = 2;
33)  }
34)  
35)  static void staticFn(){
36)    cout << "this is staticfn()\n";
37)  }
38)  //file2.h
39)  
40)  void fn();   
41)  extern int n;

```
### 题目23160
问题：1.  程序第40行的作用是什么？
A.  定义函数fn
B.  调用函数fn
C.  声明函数fn
D.  没有用


**解析如下：**

------

解析：
程序第40行的作用是声明函数 `fn`，即告诉编译器在其他地方会定义一个名为 `fn` 的函数。

{'23160':'C'}

------

### 题目23180
问题：1. 程序第41行的作用是什么？
A.  定义全局变量n
B.  声明外部变量n
C.  定义静态变量n
D.  声明静态变量n


**解析如下：**

------

解析：
程序第41行的作用是声明外部变量 `n`，即告诉编译器在其他文件中会定义一个名为 `n` 的全局变量。

{'23180':'B'}

------

### 题目23181
问题：10.关于程序第26行，下列描述中不正确的是：
A.  s是一个整型变量
B.  s可以在file2.cpp以外的其他文件中被使用
C.  s存储在全局数据区
D.  s是全局变量


**解析如下：**

------

解析：
程序第26行定义了一个静态全局变量 `s`，这意味着 `s` 只能在定义它的文件（file2.cpp）中使用，不能在其他文件中被访问。因此，选项 B 描述不正确。

{'23181':'B'}

------

### 题目23182
问题：11.程序第25行中全局变量n的初始值是多少？
A.  1
B.  -1
C.  0
D.  随机值


**解析如下：**

------

解析：
程序第25行定义了一个全局变量 `n`，在C++中，全局变量如果没有显式初始化，默认会被初始化为0。因此，全局变量 `n` 的初始值是0。

{'23182':'C'}

------

### 题目23183
问题：12.程序第26行中静态全局变量s的初始值是多少？
A.  1
B.  -1
C.  0
D.  随机值


**解析如下：**

------

解析：
程序第26行定义了一个静态全局变量 `s`，在C++中，静态全局变量如果没有显式初始化，默认会被初始化为0。因此，静态全局变量 `s` 的初始值是0。

{'23183':'C'}

------

### 题目23184
问题：13.逐语句调试时，程序从第29行将跳转到哪行？
A.  第23行
B.  第30行
C.  第33行
D.  第35行


**解析如下：**

------

解析：
程序第29行调用了 `staticFn()` 函数，由于 `staticFn()` 是在第35行定义的，因此逐语句调试时，程序将从第29行跳转到第35行。

{'23184':'D'}

------

### 题目23185
问题：14.程序第25行变量n的存储空间何时分配？
A.  编译时
B.  连接时
C.  程序执行到file1.cpp时
D.  程序执行到file2.cpp时


**解析如下：**

------

解析：
程序第25行定义了一个全局变量 `n`，全局变量的存储空间在编译时就已经分配好了，而不是在程序执行时。因此，变量 `n` 的存储空间在编译时分配。

{'23185':'A'}

------

### 题目23186
问题：15.程序第26行变量s的存储空间何时分配？
A.  编译时
B.  连接时
C.  程序执行到file1.cpp时
D.  程序执行到file2.cpp时


**解析如下：**

------

解析：
程序第26行定义了一个静态全局变量 `s`，静态全局变量的存储空间也是在编译时就已经分配好了，而不是在程序执行时。因此，变量 `s` 的存储空间在编译时分配。

{'23186':'A'}

------

### 题目23187
问题：16.逐过程调试时，程序从第29行将跳转到哪行？
A.  第23行
B.  第30行
C.  第33行
D.  第36行


**解析如下：**

------

解析：
逐过程调试（Step Over）时，程序不会进入函数内部，而是直接执行完函数调用并跳转到下一行。因此，程序从第29行调用 `staticFn()` 后，将跳转到第30行。

{'23187':'B'}

------

### 题目23188
问题：17.程序执行到第33行，将回收几个变量的存储空间？
A.  0
B.  1
C.  2
D.  4


**解析如下：**

------

解析：
程序执行到第33行时，并没有涉及到变量存储空间的回收。变量 `n` 和 `s` 是全局变量和静态全局变量，它们的存储空间在程序结束时才会被回收。因此，在第33行时，没有变量的存储空间被回收。

{'23188':'A'}

------

### 题目23189
问题：18.若删除第35行到第37行代码，程序将出现什么问题？
A.  编译报错
B.  连接报错
C.  没有问题
D.  语法错误


**解析如下：**

------

解析：
如果删除第35行到第37行的代码，即删除了 `staticFn()` 函数的定义，但第29行仍然调用了 `staticFn()` 函数。由于没有定义 `staticFn()` 函数，编译器将无法找到该函数的定义，从而导致编译报错。

{'23189':'A'}

------

### 题目23190
问题：19.解开程序第12行的注释语句，重新编译程序，将会出现什么问题？
A.  编译报错，重复定义
B.  连接报错，未找到外部引用的符号
C.  没有问题
D.  编译报错，标识符未定义


**解析如下：**

------

解析：
解开程序第12行的注释语句后，程序将尝试调用 `staticFn()` 函数。然而，`staticFn()` 是一个静态函数，其作用域仅限于定义它的文件（file2.cpp），因此在 file1.cpp 中无法调用 `staticFn()`，这将导致编译报错，标识符未定义。

{'23190':'D'}

------

### 题目23191
问题：2. 程序第38行到第41行是什么文件？
A.  头文件
B.  源文件
C.  库文件
D.  二进制文件


**解析如下：**

------

解析：
程序第38行到第41行是 `file2.h` 文件的内容，这是一个头文件（header file），用于声明函数和变量，供其他源文件包含和使用。

{'23191':'A'}

------

### 题目23192
问题：20.若删除第28行到第33行代码，程序将出现什么问题？
A.  编译报错
B.  连接报错
C.  没有问题
D.  语法错误


**解析如下：**

------

解析：
如果删除第28行到第33行的代码，即删除了 `fn()` 函数的定义，但是留下了声明，即编辑器知道`fn()` 函数的样子，即可以通过编译，但是在链接，去真正找这个函数的实现时，发现没有。

{'23192':'B'}

------

### 题目23193
问题：21.解开程序第12行的注释语句，并将第23行的函数声明语句移动到第39行处，重新编译程序，会出现什么问题？
A.  编译报错：函数staticFn()未声明
B.  连接报错
C.  编译报错：函数staticFn()声明但未定义
D.  没有问题


**解析如下：**

------

解析：
解开程序第12行的注释语句后，程序将尝试调用 `staticFn()` 函数。如果将第23行的函数声明语句移动到第39行处，即在 `file2.h` 中声明 `staticFn()`，但由于 `staticFn()` 是静态函数，其作用域仅限于定义它的文件（file2.cpp），因此在 file1.cpp 中无法调用 `staticFn()`，这将导致编译报错，函数 `staticFn()` 声明但未定义。

{'23193':'C'}

------

### 题目23194
问题：22.解开程序第14行的注释语句，重新编译程序，将会出现什么问题？
A.  编译报错，重复定义
B.  连接报错，未找到外部引用的符号
C.  没有问题
D.  编译报错，标识符未定义


**解析如下：**

------

解析：
解开程序第14行的注释语句后，程序将尝试输出静态全局变量 `s` 的值。然而，`s` 是一个静态全局变量，其作用域仅限于定义它的文件（file2.cpp），因此在 file1.cpp 中无法访问 `s`，这将导致编译报错，标识符未定义。

{'23194':'D'}

------

### 题目23195
问题：23.若删除程序第19行语句，重新编译程序，会出现什么问题？
A.  编译报错
B.  连接报错
C.  没有问题
D.  语法错误


**解析如下：**

------

解析：
没有问题，因为file2.cpp中有完整的函数定义，可以省略函数声明。

{'23195':'C'}

------

### 题目23196
问题：24.在32位系统上，程序执行结束，将释放多大存储空间？
A.  2个字节
B.  4个字节
C.  8个字节
D.  12个字节


**解析如下：**

------

解析：
在32位系统上，程序执行结束时，将释放全局变量 `n` 和静态全局变量 `s` 的存储空间。每个变量占用4个字节（32位），因此总共释放8个字节的存储空间。

{'23196':'C'}

------

### 题目23197
问题：25.编译期间会为程序第41行分配多大存储空间？
A.  不分配存储空间
B.  1个字节
C.  4个字节
D.  8个字节


**解析如下：**

------

解析：

在C或C++语言中，extern int n; 这行代码是一个声明，而不是定义。它的意思是告诉编译器，变量n是一个已经定义在其他地方的int类型的变量。这通常用于多个文件共享同一个变量时。

关于分配空间： extern int n; 本身不分配任何空间。它仅仅是一个声明，告诉编译器n这个变量在其他地方定义。

{'23197':'A'}

------

### 题目23198
问题：3. 程序第11行是什么？
A.  函数声明
B.  函数定义
C.  函数调用
D.  变量定义


**解析如下：**

------

解析：
程序第11行是 `fn();`，这是一个函数调用语句，调用了名为 `fn` 的函数。

{'23198':'C'}

------

### 题目23199
问题：4. 逐语句调试时，程序执行到第11行，接下来将跳转到哪里继续执行？
A.  第13行
B.  第28行
C.  第40行
D.  第25行


**解析如下：**

------

解析：
逐语句调试（Step Into）时，程序执行到第11行调用 `fn()` 函数后，将跳转到 `fn()` 函数的定义处继续执行。`fn()` 函数的定义在第28行。

{'23199':'B'}

------

### 题目23200
问题：5. 若删除程序第7行的语句，下列哪条语句将报错？
A.  第11行
B.  第13行
C.  第23行
D.  第28行


**解析如下：**

------

解析：
如果删除程序第7行的语句，即删除了 `#include "file2.h"`，那么在 file1.cpp 中将无法访问 `file2.h` 中声明的函数和变量。由于第11行调用了 `fn()` 函数，但没有包含头文件，编译器将无法找到 `fn()` 函数的声明，从而导致第11行报错。

{'23200':'A'}

------

### 题目23201
问题：6. 程序执行完第13行，控制台窗口输出：
A.  全局变量n=0
B.  全局变量n=1
C.  全局变量n=2
D.  全局变量n=3


**解析如下：**

------

解析：
程序执行完第13行时，控制台窗口将输出全局变量 `n` 的值。在第11行调用 `fn()` 函数后，`fn()` 函数将 `n` 的值设置为1。因此，控制台窗口将输出 `全局变量n=1`。

{'23201':'B'}

------

### 题目23202
问题：7. 若删除程序第6行，下列哪项将编译报错？
A.  第13行
B.  第14行
C.  第30行
D.  第36行


**解析如下：**

------

解析：
如果删除程序第6行的语句，即删除了 `#include <iostream>`，那么在程序中将无法使用 `std::cout` 和 `std::endl`。由于第13行使用了 `std::cout`，编译器将无法找到 `std::cout` 的定义，从而导致第13行报错。

{'23202':'A'}

------

### 题目23203
问题：8. 若删除程序第23行，下列哪项将报错？
A.  第36行
B.  第35行
C.  第29行
D.  第26行


**解析如下：**

------

解析：
如果删除程序第23行的语句，即删除了 `static void staticFn();` 的声明，那么在第29行调用 `staticFn()` 函数时，编译器将无法找到 `staticFn()` 函数的声明，从而导致第29行报错。

{'23203':'C'}

------

### 题目23204
问题：9. 关于程序第25行，下列描述中不正确的是：
A.  n是一个整型变量
B.  n是全局变量
C.  n存储在全局数据区
D.  n不可以在file2.cpp以外的其他文件中被使用


**解析如下：**

------

解析：
程序第25行定义了一个全局变量 `n`，全局变量可以在整个程序的所有文件中被使用。因此，选项 D 描述不正确。

{'23204':'D'}

------

