## ch009_007
``` c++
1)  #include <iostream>
2)  #include <iomanip>
3)  using namespace std;
4)  struct Jose//小孩结点
5)  {
6)  int code;
7)  Jose* next;
8)  };
9)  void main(){
10)  //游戏的初值
11)  int numOfBoys, interval;
12)  cout << "please input the number of boys，\n"//小孩数
          << " interval of counting:\n";  //数小孩个数
13)  cin >> numOfBoys >> interval;
14)  //定义指针
15)  Jose* pJose = 0;  //第一个小孩指针
16)  Jose* pCurrent = 0;  //当前小孩指针
17)  Jose* pivot = 0;  //前一个小孩指针
18)  int itemsInLine = 1;  //一行输出的项数
19)  //围成一个圆圈，先指定第1个小孩
20)  pJose = new Jose;
21)  pJose->code = 1;
22)  pJose->next = 0;  //后面没有小孩
23)  cout << setw(4) << pJose->code;
24)  //增加后面的小孩
25)  pivot = pJose;
26)  for (int i = 2; i <= numOfBoys; i++){
27)  pCurrent = new Jose;
28)  pCurrent->code = i;    //小孩编号
29)  pivot->next = pCurrent;  //跟在前一个小孩后面
30)  pivot = pCurrent;  //当前小孩变成前一个小孩
31)  if (itemsInLine++ % 10 == 0)
32)      cout << endl;
33)  cout << setw(4) << i;
34)  }
35)  pCurrent->next = pJose;//第1个小孩跟在最后一个小孩后面
36)  itemsInLine = 0;
37)  //开始数数离开
38)  pCurrent = pJose;    //从第一个小孩开始数
39)  while (pCurrent->next != pCurrent){  //处理未获胜的所有小孩
40)     for (int j = 0; j<interval; j++){  //数interval个小孩
41)           pivot = pCurrent;    //当前小孩变成前一个小孩
42)           pCurrent = pivot->next;//后一个小孩变成当前小孩
43)     }
44)   if (itemsInLine++ % 10 == 0)   //输出小孩
45)      cout << endl;
46)   cout << setw(4) << pCurrent->code;
47)   pivot->next = pCurrent->next;  //当前小孩的一个小孩跟在他前一个的后面
48)   delete pCurrent;  //脱离圆圈后删除
49)   pCurrent = pivot;  //当前小孩变成前一个小孩
50)  }
51)  cout << "\n\nthe winner is "<< pCurrent->code << endl;     //获胜者
52)  delete pCurrent;
53)  system("pause");
54)  }

```
### 题目24006
问题：1.  输入5 2后，小孩的位置是怎样的：
A.  1<-5<-4<-3<-2<-1
B.  1->5->4->3->2->1
C.  5<-4<-3<-2<-1<-5
D.  1->5->3->2->4->1


**解析如下：**

------
1->2->4->1。

**解析：**
1. 程序首先创建一个循环链表，每个节点代表一个小孩，编号从1到numOfBoys。
2. 输入5 2后，程序会创建一个包含5个小孩的循环链表，编号依次为1, 2, 3, 4, 5。 
即 1<-5<-4<-3<-2<-1

根据以上分析，正确答案是：
{'24006':'A'}

------

### 题目24007
问题：2.  输入5 2后，执行完43行，pCurrent、pJose、pivot的值：
A.  1 2 4
B.  4 5 1
C.  5 1 2
D.  5 1 4


**解析如下：**

------

解析：
不太清楚题目的意思，题库给的答案是D.

根据以上分析，正确答案是：
{'24007':'D'}

------

### 题目24008
问题：3.  setw()函数的作用：
A.  控制位宽
B.  控制行距
C.  控制小数点位数
D.  控制字距


**解析如下：**

------

解析：
1. setw()函数是C++标准库中的一个函数，用于设置输出流的宽度。
2. 它通常用于格式化输出，控制输出字段的宽度。
3. 在程序中，setw(4)用于设置输出字段的宽度为4个字符。

根据以上分析，正确答案是：
{'24008':'A'}

------

### 题目24009
问题：4.  删除节点时怎么做的：
A.  断链->删除->连接->更新当前节点
B.  更新当前节点->断链->连接->删除
C.  断链->连接->删除->更新当前节点
D.  删除->连接->更新当前节点->断链 


**解析如下：**

------

解析：
链表的删除 
pivot->next = pCurrent->next;  //断链->连接
delete pCurrent;  //脱离圆圈后删除
pCurrent = pivot;  //更新当前节点

根据以上分析，正确答案是：
{'24009':'C'}

------

### 题目24010
问题：5.  输入5 2后，39行的while将会执行几次：
A.  1
B.  2
C.  4
D.  5


**解析如下：**

------

解析：
1. 输入5 2后，程序会创建一个包含5个小孩的循环链表，编号依次为1, 2, 3, 4, 5。
2. 链表的结构是：1->2->3->4->5->1。
3. 程序从编号1的小孩开始，每隔interval（这里是2）个小孩，移除一个小孩。
4. 39行的while循环条件是pCurrent->next != pCurrent，即链表中只剩下一个小孩时循环结束。
5. 每次循环移除一个小孩，直到只剩下一个小孩。
6. 因此，while循环将会执行4次，每次移除一个小孩，直到链表中只剩下一个小孩。

根据以上分析，正确答案是：
{'24010':'C'}

------

### 题目24011
问题：6.  输入5 2后，哪个编号的节点首先被删除了：
A.  1
B.  2
C.  5
D.  3


**解析如下：**

------

解析：
1. 输入5 2后，程序会创建一个包含5个小孩的循环链表，编号依次为1, 2, 3, 4, 5。
2. 链表的结构是：1->2->3->4->5->1。
                 pv pc  删除pc

根据以上分析，正确答案是：
{'24011':'D'}

------

### 题目24012
问题：7.  输入5 2后，第一次删除节点后，执行到50行，pCurrent的值是：
A.  1
B.  2
C.  5
D.  3


**解析如下：**

------

解析：
输入5,2第一次删除了3，最后pCurrent=pvoit=2

根据以上分析，正确答案是：
{'24012':'B'}

------

### 题目24013
问题：8.  输入5 2后，获胜者是：
A.  1
B.  2
C.  4
D.  5


**解析如下：**

------

解析：
1. 输入5 2后，程序会创建一个包含5个小孩的循环链表，编号依次为1, 2, 3, 4, 5。
2. 链表的结构是：1->2->3->4->5->1。
3. 程序从编号1的小孩开始，每隔interval（这里是2）个小孩，移除一个小孩。
4. 第一次循环，从1开始数到3，然后移除3号小孩。
5. 第二次循环，从2开始数到5，然后移除5号小孩。
6. 第三次循环，从4开始数到2，然后移除2号小孩。
7. 第四次循环，从1开始数到1，然后移除1号小孩。
8. 最后只剩下4号小孩，获胜者是4号小孩。

根据以上分析，正确答案是：
{'24013':'C'}

------

### 题目24014
问题：9.  输入6 3后，获胜者是：
A.  1
B.  2
C.  4
D.  6


**解析如下：**

------

解析：
1. 输入6 3后，程序会创建一个包含6个小孩的循环链表，编号依次为1, 2, 3, 4, 5, 6。
2. 链表的结构是：1->2->3->4->5->6->1。
3. 程序从编号1的小孩开始，每隔interval（这里是3）个小孩，移除一个小孩。
4. 第一次循环，从1开始数到4，然后移除4号小孩。
5. 第二次循环，从3开始数到1，然后移除1号小孩。
6. 第三次循环，从6开始数到5，然后移除5号小孩。
7. 第四次循环，从3开始数到6，然后移除6号小孩。
8. 第五次循环，从3开始数到2，然后移除3号小孩。
9. 最后只剩下2号小孩，获胜者是2号小孩。

根据以上分析，正确答案是：
{'24014':'B'}

------

