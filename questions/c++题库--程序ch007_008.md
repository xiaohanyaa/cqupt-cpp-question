## ch007_008
``` c++
1)  #include<iostream>
2)  using namespace std;
3)  void bubble(int[], int);
4)  void print(int array[], int len);
5)  void main(){
6)    int array[] = { 55, 2, 6, 4, 32, 12, 9, 73, 26, 37 };
7)    int len = sizeof(array) / sizeof(int);    //元素个数
8)    print(array, len);
9)    bubble(array, len);      //调用排序函数
10)    print(array, len);
11)    system("pause");
12)  }
13)  void print(int array[], int len){
14)    for (int i = 0; i<len; i++)              //原始顺序输出
15)     cout << array[i] << ",";
16)     cout << endl << endl;
17)  }
18)  void bubble(int a[], int size)    //冒泡排序
19)  {
20)    int i, temp;
21)    for (int pass = 1; pass<size; pass++){    //共比较size-1轮
22)      for (i = 0; i<size - pass; i++) {          //比较一轮
23)        if (a[i]>a[i + 1]){
24)         temp = a[i];
25)         a[i] = a[i + 1];
26)         a[i + 1] = temp;
27)        }
28)     }
29)     print(a, size);
30)    }
31)  }

```
### 题目23640
问题：程序第6行中数组array的长度为：

A、0
B、9
C、10
D、11


**解析如下：**

------

解析：
在程序的第7行，`int len = sizeof(array) / sizeof(int);` 这行代码计算了数组 `array` 的长度。`sizeof(array)` 返回的是整个数组占用的字节数，而 `sizeof(int)` 返回的是一个 `int` 类型占用的字节数。因此，`sizeof(array) / sizeof(int)` 的结果就是数组中元素的个数。

根据第6行的数组定义 `int array[] = { 55, 2, 6, 4, 32, 12, 9, 73, 26, 37 };`，数组中有10个元素。

因此，数组 `array` 的长度为10。

{'23640':'C'}

------

### 题目23641
问题：如果将程序第6行改为int array[5]={55,2,6,4,32,12,9,73,26,37}，则程序执行完第15行后输出的结果为：

A、55,2,6,4,32,12,9,73,26,37
B、55,2,6,4,32
C、12,9,73,26,37
D、程序出现错误


**解析如下：**

------

解析：
如果将程序第6行改为 `int array[5] = {55, 2, 6, 4, 32, 12, 9, 73, 26, 37};`，这行代码会导致编译错误。因为在C++中，数组的初始化列表中的元素数量不能超过数组声明的大小。这里声明的数组大小为5，但初始化列表中有10个元素，这超出了数组的容量。

因此，程序在编译时会出现错误，无法执行到第15行。

{'23641':'D'}

------

### 题目23642
问题：如果将程序第6行改为int array[11]={55,2,6,4,32,12,9,73,26,37}，则程序执行完第15行后输出的结果为：

A、55,2,6,4,32,12,9,73,26,37
B、55,2,6,4,32,12,9,73,26,37,0
C、0,55,2,6,4,32,12,9,73,26,37
D、程序出现错误


**解析如下：**

------

解析：
如果将程序第6行改为 `int array[11] = {55, 2, 6, 4, 32, 12, 9, 73, 26, 37};`，数组 `array` 的大小为11，但初始化列表中只有10个元素。在C++中，未显式初始化的数组元素会被自动初始化为0。

因此，数组 `array` 的内容将是 `{55, 2, 6, 4, 32, 12, 9, 73, 26, 37, 0}`。

程序执行完第15行后，输出的结果将是：
```
55,2,6,4,32,12,9,73,26,37,0,
```

所以，正确答案是B。

{'23642':'B'}

------

### 题目23643
问题：在32位计算机中，程序执行到第6行时，数组array的存储空间大小是：

A、10个字节
B、20个字节
C、30个字节
D、40个字节


**解析如下：**

------

解析：
在32位计算机中，一个 `int` 类型占用4个字节。程序第6行定义的数组 `array` 包含10个 `int` 类型的元素。

因此，数组 `array` 的存储空间大小为：
```
10个元素 * 每个元素4个字节 = 40个字节
```

所以，正确答案是D。

{'23643':'D'}

------

### 题目23644
问题：程序执行完第6行之后，数组array[0]的值是：

A、0
B、55
C、随机值
D、以上答案都不正确


**解析如下：**

------

解析：
程序第6行定义并初始化了数组 `array`，具体代码如下：
```cpp
int array[] = { 55, 2, 6, 4, 32, 12, 9, 73, 26, 37 };
```

在这个初始化列表中，数组的第一个元素 `array[0]` 被显式地初始化为55。

因此，程序执行完第6行之后，数组 `array[0]` 的值是55。

所以，正确答案是B。

{'23644':'B'}

------

### 题目23645
问题：程序第6行中，数组array[]的最后一个元素是：

A、array[9]
B、array[10]
C、array[11]
D、以上答案都不正确


**解析如下：**

------

解析：
程序第6行定义并初始化了数组 `array`，具体代码如下：
```cpp
int array[] = { 55, 2, 6, 4, 32, 12, 9, 73, 26, 37 };
```

在这个初始化列表中，数组 `array` 包含10个元素。数组的索引从0开始，因此最后一个元素的索引是9。

所以，数组 `array` 的最后一个元素是 `array[9]`。

因此，正确答案是A。

{'23645':'A'}

------

### 题目23646
问题：程序执行完第7行后，变量len的值是：

A、9
B、10
C、11
D、随机值


**解析如下：**

------

解析：
程序第7行计算了数组 `array` 的长度，具体代码如下：
```cpp
int len = sizeof(array) / sizeof(int);
```

在第6行中，数组 `array` 被定义并初始化为10个元素：
```cpp
int array[] = { 55, 2, 6, 4, 32, 12, 9, 73, 26, 37 };
```

`sizeof(array)` 返回的是整个数组占用的字节数，而 `sizeof(int)` 返回的是一个 `int` 类型占用的字节数。因此，`sizeof(array) / sizeof(int)` 的结果就是数组中元素的个数。

由于数组 `array` 有10个元素，因此 `len` 的值将是10。

所以，正确答案是B。

{'23646':'B'}

------

### 题目23647
问题：程序刚开始执行第9行时，变量i的值为： 

A、变量i不存在
B、i=11
C、i值为无穷大
D、i值为0


**解析如下：**

------

解析：
程序第9行调用了 `bubble` 函数，具体代码如下：
```cpp
bubble(array, len);
```

在 `bubble` 函数中，变量 `i` 是在第22行定义的：
```cpp
for (i = 0; i < size - pass; i++) {
```

在 `bubble` 函数刚开始执行时，变量 `i` 还没有被定义和初始化。因此，变量 `i` 不存在。

所以，正确答案是A。

{'23647':'A'}

------

### 题目23648
问题：程序执行到第18行时，变量size的值为：

A、随机值
B、0
C、10
D、以上答案都不正确


**解析如下：**

------

解析：
程序第18行是 `bubble` 函数的定义，具体代码如下：
```cpp
void bubble(int a[], int size)
```

在第9行，`bubble` 函数被调用，并传递了两个参数：
```cpp
bubble(array, len);
```

其中，`array` 是数组，`len` 是数组的长度。在第7行，`len` 被计算为数组 `array` 的长度，即10：
```cpp
int len = sizeof(array) / sizeof(int);
```

因此，当程序执行到第18行时，变量 `size` 的值为10。

所以，正确答案是C。

{'23648':'C'}

------

### 题目23649
问题：程序执行完第20行后，变量i的值为：

A、0
B、10
C、11
D、随机值


**解析如下：**

------

解析：
程序第20行定义了变量 `i`，具体代码如下：
```cpp
int i, temp;
```

在这行代码中，变量 `i` 被定义但没有被初始化。在C++中，局部变量如果没有显式初始化，其值是未定义的，通常是随机值。

因此，程序执行完第20行后，变量 `i` 的值为随机值。

所以，正确答案是D。

{'23649':'D'}

------

### 题目23650
问题：程序第21行中变量pass的作用范围在哪一行结束：

A、第27行
B、第28行
C、第30行
D、第31行


**解析如下：**

------

解析：
程序第21行定义了变量 `pass`，具体代码如下：
```cpp
for (int pass = 1; pass < size; pass++) {
```

在这个 `for` 循环中，变量 `pass` 的作用范围仅限于这个循环体内。循环体内的代码从第21行开始，到第30行结束。

因此，变量 `pass` 的作用范围在第30行结束。

所以，正确答案是C。

{'23650':'C'}

------

### 题目23651
问题：程序第21行中变量pass的值为1时，则第22行中的for循环要执行多少次：

A、1次
B、9次
C、10次
D、不确定


**解析如下：**

------

解析：
程序第21行定义了变量 `pass`，具体代码如下：
```cpp
for (int pass = 1; pass < size; pass++) {
```

当 `pass` 的值为1时，第22行的 `for` 循环将执行：
```cpp
for (i = 0; i < size - pass; i++) {
```

在这个循环中，`size` 的值为10（因为 `size` 是数组的长度），所以 `size - pass` 的值为9。

因此，当 `pass` 的值为1时，第22行的 `for` 循环将执行9次。

所以，正确答案是B。

{'23651':'B'}

------

### 题目23652
问题：程序第21行中变量pass的值为1时，程序执行第29行调用print()函数，当第15行执行完后，输出的结果是：

A、2,6,4,32,12,9,55,26,37,73
B、2,4,6,32,12,9,55,26,37,73
C、2,6,4,32,55,12,9,26,37,73
D、2,6,4,32,55,9,12,26,37,73


**解析如下：**

------

解析：
程序第21行定义了变量 `pass`，具体代码如下：
```cpp
for (int pass = 1; pass < size; pass++) {
```

当 `pass` 的值为1时，第22行的 `for` 循环将执行：
```cpp
for (i = 0; i < size - pass; i++) {
```

在这个循环中，`size` 的值为10，所以 `size - pass` 的值为9。循环将执行9次，每次比较相邻的两个元素，如果前一个元素大于后一个元素，则交换它们的位置。

在第一次循环结束后，最大的元素会被移动到数组的最后一个位置。因此，数组 `array` 的内容将变为：
```
2, 6, 4, 32, 12, 9, 55, 26, 37, 73
```

当第29行调用 `print` 函数时，输出的结果将是：
```
2, 6, 4, 32, 12, 9, 55, 26, 37, 73
```

所以，正确答案是A。

{'23652':'A'}

------

### 题目23653
问题：程序第21行中变量pass的值为9时，程序执行第29行调用print()函数，当第15行执行完后，输出的结果是：

A、2,6,4,32,12,9,55,26,37,73
B、2,4,6,12,9,32,26,37,55,73
C、2,4,6,12,32,9,55,26,37,73
D、2,4,6,9,12,26,32,37,55,73


**解析如下：**

------

解析：
程序第21行定义了变量 `pass`，具体代码如下：
```cpp
for (int pass = 1; pass < size; pass++) {
```

当 `pass` 的值为9时，第22行的 `for` 循环将执行：
```cpp
for (i = 0; i < size - pass; i++) {
```

在这个循环中，`size` 的值为10，所以 `size - pass` 的值为1。循环将执行1次，比较数组的第一个和第二个元素，如果前一个元素大于后一个元素，则交换它们的位置。

在第9次循环结束后，数组 `array` 的内容将变为：
```
2, 4, 6, 9, 12, 26, 32, 37, 55, 73
```

当第29行调用 `print` 函数时，输出的结果将是：
```
2, 4, 6, 9, 12, 26, 32, 37, 55, 73
```

所以，正确答案是D。

{'23653':'D'}

------

### 题目23654
问题：程序要执行完第14行语句中的for和执行完第21行的语句后的for循环，分别需要执行多少次：

A、9次，90次
B、10次，90次
C、9次，45次
D、10次，45次


**解析如下：**

------

解析：
程序第14行是一个 `for` 循环，具体代码如下：
```cpp
for (int i = 0; i < len; i++)
```

在这个循环中，`len` 的值为10（因为 `len` 是数组的长度），所以这个循环将执行10次。

程序第21行也是一个 `for` 循环，具体代码如下：
```cpp
for (int pass = 1; pass < size; pass++) {
```

在这个循环中，`size` 的值为10，所以这个循环将执行9次。在每次循环中，还有一个嵌套的 `for` 循环：
```cpp
for (i = 0; i < size - pass; i++) {
```

这个嵌套的 `for` 循环在每次外层循环中执行的次数分别是：
- 第1次：10 - 1 = 9次
- 第2次：10 - 2 = 8次
- 第3次：10 - 3 = 7次
- 第4次：10 - 4 = 6次
- 第5次：10 - 5 = 5次
- 第6次：10 - 6 = 4次
- 第7次：10 - 7 = 3次
- 第8次：10 - 8 = 2次
- 第9次：10 - 9 = 1次

因此，嵌套的 `for` 循环总共执行的次数为：
```
9 + 8 + 7 + 6 + 5 + 4 + 3 + 2 + 1 = 45次
```

所以，程序要执行完第14行语句中的 `for` 循环和执行完第21行的 `for` 循环，分别需要执行10次和45次。

正确答案是D。

{'23654':'D'}

------

