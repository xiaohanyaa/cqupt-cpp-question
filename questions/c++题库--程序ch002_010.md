## ch002_010
``` c++
1) #include <iostream>
2) #include <iomanip>
3) using namespace std;
4) int main()
5) {
6)    unsigned short us = 65535;
7)    us = us + 1;
8)    cout << " unsigned short:";
9)    cout << setw(8) << hex << us << "," << setw(8) << dec << us << endl;
10)  signed short ss = 32767;
11)  ss = ss + 1;
12)  cout << " signed short:";
13)   cout << setw(8) << hex << ss << "," << setw(8) << dec << ss << endl;
14)  return 0;
15) }

```
### 题目22807
问题：1. 执行该程序，程序有几行输出：
A.  1
B. 2
C. 3
D.  4


**解析如下：**

------

解析：
该程序中，第9行和第13行分别进行了两次输出操作，每次输出都会产生一行输出。因此，程序总共有两行输出。

{'22807':'B'}

------

### 题目22808
问题：2.变量unsigned short us = 65535在内存中存储的是：
A.f
B.ff
C.fff
D.ffff


**解析如下：**

------

解析：
unsigned short类型在大多数系统中占用2个字节（16位），其最大值为65535。在内存中，65535的十六进制表示为0xFFFF。因此，变量us在内存中存储的是0xFFFF，即四个十六进制字符'F'。

{'22808':'D'}

------

### 题目22809
问题：3.变量signed short ss = 32767在内存中存储的是：
A.ff
B.7fff
C.8fff
D.8000


**解析如下：**

------

解析：
signed short类型在大多数系统中占用2个字节（16位），其最大正值为32767。在内存中，32767的十六进制表示为0x7FFF。因此，变量ss在内存中存储的是0x7FFF，即四个十六进制字符'7FFF'。

{'22809':'B'}

------

### 题目22810
问题：4.执行该程序，屏幕输出第一行为：
A.  unsigned short:       0,       0
B. unsigned short:       0,       65536
C. unsigned short:       65536,       0
D.  unsigned short:       65536,   65536


**解析如下：**

------

解析：
在第7行，变量us的值为65535，加上1后会溢出，因为unsigned short类型的最大值是65535。溢出后，us的值会回到0。因此，第9行的输出将是：

```
unsigned short:       0,       0
```

{'22810':'A'}

------

### 题目22811
问题：5.执行该程序，屏幕输出第二行为：
A. signed short:    -32768,  -32768
B. signed short:    8000,    8000
C. signed short:    8000,    -32768 
D. signed short:    -32768， 8000


**解析如下：**

------

解析：
在第11行，变量ss的值为32767，加上1后会溢出，因为signed short类型的最大正值是32767。溢出后，ss的值会变成-32768，其十六进制表示为0x8000。因此，第13行的输出将是：

```
signed short:       8000,     -32768
```

{'22811':'C'}

------

### 题目22812
问题：6.把程序第6行修改为unsigned short us = -2，执行该程序，屏幕输出第一行为：
A.  unsigned short:    ffff,     -1
B. unsigned short:    -1,     ffff
C. unsigned short:    ffff,     65535
D.  unsigned short:    65535,  fff


**解析如下：**

------

解析：
将第6行修改为`unsigned short us = -2;`，由于unsigned short类型不能存储负数，-2会被转换为无符号数。在16位无符号整数中，-2的表示为65534，其十六进制表示为0xFFFE。因此，第9行的输出将是：

```
unsigned short:       fffe,     65534
```

{'22812':'C'}

------

### 题目22813
问题：7.把程序第10行修改为signed short ss = -2，执行该程序，屏幕输出第二行为：
A.  unsigned short:    ffff,     -1
B. unsigned short:    -1,     ffff
C. unsigned short:    ffff,     65535
D.  unsigned short:    65535,  fff


**解析如下：**

------

解析：
将第10行修改为`signed short ss = -2;`，由于signed short类型可以存储负数，-2的十六进制表示为0xFFFE。在第11行，ss的值加上1后会变成-1，其十六进制表示为0xFFFF。因此，第13行的输出将是：
这里题目有点问题，不管，选择最正确答案即可！
```
signed short:       ffff,     -1
```

{'22813':'A'}

------

### 题目22814
问题：8.把程序第10行修改为signed short ss = 65535，执行该程序，屏幕输出第二行为：
A.  signed short:    ffff,     -1
B. signed short:    -1,     ffff
C. signed short:    0,     0
D.  signed short:    ffff,  0


**解析如下：**

------

解析：
将第10行修改为`signed short ss = 65535;`，由于signed short类型不能存储65535，65535会被截断为-1，因为65535的二进制表示为1111111111111111，这正是-1的补码表示。在第11行，ss的值加上1后会变成0，其十六进制表示为0x0000。因此，第13行的输出将是：

```
signed short:       0,     0
```

{'22814':'C'}

------

### 题目22815
问题：9.signed short ss = 65535按照十进制输出，结果是：
A.65535
B.-1
C.65536
D.  0


**解析如下：**

------

解析：
signed short类型在大多数系统中占用2个字节（16位），其最大正值为32767，最小负值为-32768。当赋值65535时，由于signed short类型不能存储65535，65535会被截断为-1，因为65535的二进制表示为1111111111111111，这正是-1的补码表示。因此，按照十进制输出，结果是-1。

{'22815':'B'}

------

### 题目22816
问题：10.函数setw的作用是:
A.  没作用
B. 设定输入输出精度
C. 设定输入输出宽度
D.  设定输入输出范围


**解析如下：**

------

解析：
函数`setw`是C++标准库中的一个流操纵符，用于设置输入输出流的宽度。它指定了下一个输入或输出操作的字段宽度。因此，`setw`的作用是设定输入输出宽度。

{'22816':'C'}

------

### 题目22817
问题：11. hex的作用是：
A.  使用二进制格式输出
B.  使用八进制格式输出
C.  使用十进制格式输出
D.  使用十六进制格式输出


**解析如下：**

------

解析：
`hex`是C++标准库中的一个流操纵符，用于将后续的整数输出格式化为十六进制格式。因此，`hex`的作用是使用十六进制格式输出。

{'22817':'D'}

------

### 题目22818
问题：12. dec的作用是：
A.  使用二进制格式输出
B.  使用八进制格式输出
C.  使用十进制格式输出
D.  使用十六进制格式输出


**解析如下：**

------

解析：
`dec`是C++标准库中的一个流操纵符，用于将后续的整数输出格式化为十进制格式。因此，`dec`的作用是使用十进制格式输出。

{'22818':'C'}

------

### 题目22819
问题：13. oct的作用是：
A.  使用二进制格式输出
B.  使用八进制格式输出
C.  使用十进制格式输出
D.  使用十六进制格式输出


**解析如下：**

------

解析：
`oct`是C++标准库中的一个流操纵符，用于将后续的整数输出格式化为八进制格式。因此，`oct`的作用是使用八进制格式输出。

{'22819':'B'}

------

