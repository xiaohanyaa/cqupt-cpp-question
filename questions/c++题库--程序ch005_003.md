## ch005_003
``` c++
1)#include <iostream>
2)using namespace std;
3)void func();
4)int n = 1;    
5)void main()
6){
7)  static int a;    
8)  int b = -10;      
9)  cout << "a:" << a
10)    << "  b:" << b
11)    << "  n:" << n << endl;
12)  b += 4;
13)  func();
14)  cout << "a:" << a
15)    << "  b:" << b
16)    << "  n:" << n << endl;
17)  n += 10;
18)  func();
19)}
20)void func()
21){
22)  static int a = 2;    
23)  int b = 5;           
24)  a += 2;
25)  n += 12;
26)  b += 5;
27)  cout << "a:" << a
28)    << "  b:" << b
29)    << "  n:" << n << endl;
30)}

```
### 题目23055
问题：16.  该程序第7行，错误的描述是：
A. 变量a存储在内存的全局数据区
B. 变量a的数据类型是int
C. 定义了一个整型变量a，未初始化
D. 变量a只能在main函数中使用


**解析如下：**

------

解析：
- 第7行定义了一个静态局部变量 `a`，其存储在内存的全局数据区（A正确）。
- 变量 `a` 的数据类型是 `int`（B正确）。
- 定义了一个整型变量 `a`，未初始化 -- 错误。（静态变量会自己初始化为该类型的默认值，int为0）。
- 变量 `a` 是静态局部变量，只能在 `main` 函数中使用（D正确 ）。

{'23055':'C'}

------

### 题目23056
问题：17.  执行该程序第12行，变量b的结果是：
A. -10
B. 10
C. -6
D. 6


**解析如下：**

------

解析：
- 第12行代码 `b += 4;` 表示将变量 `b` 的值增加 4。
- 在第8行，变量 `b` 被初始化为 `-10`。
- 因此，执行第12行后，`b` 的值为 `-10 + 4 = -6`。

{'23056':'C'}

------

### 题目23057
问题：18.  该程序执行完第13行后，错误的描述是：
A. 该程序跳转至第21行
B. func函数没有返回值
C. func函数没有参数
D. 变量n不能在函数fun中使用


**解析如下：**

------

解析：
- 第13行调用了 `func()` 函数，程序会跳转至第21行执行 `func` 函数（A正确）。
- `func` 函数是一个 `void` 类型的函数，没有返回值（B正确）。
- `func` 函数没有参数（C正确）。
- 变量 `n` 是全局变量，可以在 `func` 函数中使用（D错误）。

{'23057':'D'}

------

### 题目23058
问题：19.  该程序第22行，错误的描述是：
A. 变量a存储在内存的全局数据区
B. 变量a的数据类型是int
C. 将第7行的变量a从0变为2
D. 变量a在全局数据区有2个


**解析如下：**

------

解析：
- 第22行定义了一个静态局部变量 `a`，其存储在内存的全局数据区（A正确）。
- 变量 `a` 的数据类型是 `int`（B正确）。
- 第22行的 `a` 是 `func` 函数中的静态局部变量，与第7行的 `a` 是不同的变量，不会影响第7行的 `a`（C错误）。
- 全局数据区中确实有两个 `a`，一个是 `main` 函数中的静态局部变量，另一个是 `func` 函数中的静态局部变量（D正确）。

{'23058':'C'}

------

### 题目23059
问题：20.  执行该程序第24行，变量a的结果是：
A. 6
B. 4
C. 2
D. 0


**解析如下：**

------

解析：
- 第22行定义了一个静态局部变量 `a`，并初始化为 `2`。
- 第24行代码 `a += 2;` 表示将变量 `a` 的值增加 `2`。
- 因此，执行第24行后，`a` 的值为 `2 + 2 = 4`。

{'23059':'B'}

------

### 题目23060
问题：21.  执行该程序第25行，变量n的结果是：
A. 13
B. 12
C. 0
D. 1


**解析如下：**

------

解析：
- 第25行代码 `n += 12;` 表示将全局变量 `n` 的值增加 `12`。
- 在第4行，全局变量 `n` 被初始化为 `1`。
- 因此，执行第25行后，`n` 的值为 `1 + 12 = 13`。

{'23060':'A'}

------

### 题目23061
问题：22.  执行该程序第26行，变量b的结果是：
A. -1
B. 4
C. 5
D. 10


**解析如下：**

------

解析：
- 第23行定义了一个局部变量 `b`，并初始化为 `5`。
- 第26行代码 `b += 5;` 表示将变量 `b` 的值增加 `5`。
- 因此，执行第26行后，`b` 的值为 `5 + 5 = 10`。

{'23061':'D'}

------

### 题目23062
问题：23.  当执行完函数func后，第14行a显示结果为：
A. 0
B. 2
C. 4
D. 6


**解析如下：**

------

解析：
- 第7行定义了一个静态局部变量 `a`，并初始化为 `0`。
- 第14行输出的是 `main` 函数中的静态局部变量 `a`，其值在 `main` 函数中没有被修改，仍然是 `0`。

{'23062':'A'}

------

### 题目23063
问题：24.  当执行完函数func后，第15行b显示结果为：
A. 10
B. 5
C. -1
D. -6


**解析如下：**

------

解析：
- 第8行定义了一个局部变量 `b`，并初始化为 `-10`。
- 第12行代码 `b += 4;` 将 `b` 的值增加 `4`，因此 `b` 的值变为 `-6`。
- 第15行输出的是 `main` 函数中的局部变量 `b`，其值在 `main` 函数中没有被进一步修改，仍然是 `-6`。

{'23063':'D'}

------

### 题目23064
问题：25.  第17行执行完后n的结果为：
A. 13
B. 23
C. 10
D. 1


**解析如下：**

------

解析：
- 第17行代码 `n += 10;` 表示将全局变量 `n` 的值增加 `10`。
- 在第25行，全局变量 `n` 的值已经被增加到 `13`。
- 因此，执行第17行后，`n` 的值为 `13 + 10 = 23`。

{'23064':'B'}

------

### 题目23065
问题：26.  当再次执行函数func时，22行的变量a的值为：
A. 2
B. 4
C. 0
D. 6


**解析如下：**

------

解析：
- 第22行定义了一个静态局部变量 `a`，并初始化为 `2`。
- 静态局部变量在函数调用之间保持其值，因此第一次调用 `func` 后，`a` 的值被增加到 `4`。
- 再次调用 `func` 时，`a` 的值仍然是 `4`，因为静态局部变量不会重新初始化。

{'23065':'B'}

------

### 题目23066
问题：27.  当再次执行函数func时，22行的变量a的值为：
A. 2
B. 4
C. 0
D. 6


**解析如下：**

------

解析：
- 第22行定义了一个静态局部变量 `a`，并初始化为 `2`。
- 静态局部变量在函数调用之间保持其值，因此第一次调用 `func` 后，`a` 的值被增加到 `4`。
- 再次调用 `func` 时，`a` 的值仍然是 `4`，因为静态局部变量不会重新初始化。

{'23066':'B'}

------

### 题目23067
问题：28.  当再次执行函数func时，24行的变量a的值为：
A. 2
B. 4
C. 5
D. 6


**解析如下：**

------

解析：
- 第22行定义了一个静态局部变量 `a`，并初始化为 `2`。
- 静态局部变量在函数调用之间保持其值，因此第一次调用 `func` 后，`a` 的值被增加到 `4`。
- 再次调用 `func` 时，`a` 的值仍然是 `4`，因为静态局部变量不会重新初始化。
- 第24行代码 `a += 2;` 将 `a` 的值增加 `2`，因此 `a` 的值变为 `4 + 2 = 6`。

{'23067':'D'}

------

### 题目23068
问题：29.  当再次执行函数func后，变量n，b的分别值为：
A. 35,10
B. 23，10
C. 35，-1
D. 23，-1


**解析如下：**

------

解析：
- 第17行执行后，全局变量 `n` 的值为 `23`。
- 第25行代码 `n += 12;` 将 `n` 的值增加 `12`，因此 `n` 的值变为 `23 + 12 = 35`。
- 第23行定义了一个局部变量 `b`，并初始化为 `5`。
- 第26行代码 `b += 5;` 将 `b` 的值增加 `5`，因此 `b` 的值变为 `5 + 5 = 10`。
- 局部变量 `b` 在 `func` 函数中被修改，但不会影响 `main` 函数中的 `b`，因此 `main` 函数中的 `b` 仍然是 `-6`。

{'23068':'A'}

------

### 题目23069
问题：30.  下列关于static描述错误的是：
A. 它是定义静态变量的关键字
B. 它存储在全局数据区
C. 它能多次被初始化
D. 它只能在定义变量的函数中使用


**解析如下：**

------

解析：
- `static` 是定义静态变量的关键字（A正确）。
- 静态变量存储在全局数据区（B正确）。
- 静态变量在程序运行期间只会被初始化一次，不能多次被初始化（C错误）。
- 静态局部变量只能在定义变量的函数中使用（D正确）。

{'23069':'C'}

------

### 题目23070
问题：31.  下列关于该程序描述错误的是：
A. 变量n是全局变量
B. 第3行表示定义了一个函数func
C. 该程序调用了2次func函数
D. Func函数无返回值


**解析如下：**

------

解析：
- 变量 `n` 是全局变量（A正确）。
- 第3行表示**声明**了一个函数 `func`（B错误）。
- 该程序在第13行和第18行分别调用了 `func` 函数，共调用了2次（C正确）。
- `func` 函数是一个 `void` 类型的函数，没有返回值（D正确）。

理解声明和定义的区别，前者只是说了有这个东西，后者是指明了这个东西是什么样子的

{'23070':'B'}

------

### 题目23071
问题：32.  下列关于静态变量，全局变量，局部变量描述错误的是：
A. 全局变量是所有函数都能使用的变量
B. 全局变量和静态变量的内存空间，程序结束后系统回收
C. 局部变量和静态局部变量的内存空间，函数结束时系统回收
D. 静态局部变量只能够定义它的函数使用


**解析如下：**

------

解析：
- 全局变量是所有函数都能使用的变量（A正确）。
- 全局变量和静态变量的内存空间在程序结束后由系统回收（B正确）。
- 局部变量的内存空间在函数结束时由系统回收，但静态局部变量的内存空间在程序结束后由系统回收（C错误）。
- 静态局部变量只能够定义它的函数使用（D正确）。

{'23071':'C'}

------

### 题目23072
问题：33.  当第8行改为static int b = -10，执行第15行b的值为：
A. -10
B. 10
C. 4
D. -6


**解析如下：**

------

解析：
- 如果第8行改为 `static int b = -10;`，则 `b` 成为静态局部变量。
- 静态局部变量在函数调用之间保持其值，因此 `b` 的初始值为 `-10`。
- 第12行代码 `b += 4;` 将 `b` 的值增加 `4`，因此 `b` 的值变为 `-10 + 4 = -6`。
- 第15行输出的是 `main` 函数中的静态局部变量 `b`，其值在 `main` 函数中没有被进一步修改，仍然是 `-6`。

{'23072':'D'}

------

### 题目23073
问题：34.  当第8行和第23行都在int前面加上static后，执行第15行b的值为，该函数中b的值为：
A. -10
B. 10
C. -6
D. 4


**解析如下：**

------

解析：
- 如果第8行和第23行都在 `int` 前面加上 `static`，则 `b` 成为静态局部变量。
- 第8行定义的静态局部变量 `b` 在 `main` 函数中初始化为 `-10`。
- 第12行代码 `b += 4;` 将 `b` 的值增加 `4`，因此 `b` 的值变为 `-10 + 4 = -6`。
- 第15行输出的是 `main` 函数中的静态局部变量 `b`，其值在 `main` 函数中没有被进一步修改，仍然是 `-6`。
- 第23行定义的静态局部变量 `b` 在 `func` 函数中初始化为 `5`，与 `main` 函数中的 `b` 是不同的变量。

{'23073':'C'}

------

### 题目23074
问题：35.  当第8行和第23行都在int前面加上static后，执行第18行func函数后，该函数中的b的值为：
A. 15
B. 10
C. 5
D. 20


**解析如下：**

------

解析：
- 如果第8行和第23行都在 `int` 前面加上 `static`，则 `b` 成为静态局部变量。
- 第23行定义的静态局部变量 `b` 在 `func` 函数中初始化为 `5`。
- 第26行代码 `b += 5;` 将 `b` 的值增加 `5`，因此 `b` 的值变为 `5 + 5 = 10`。
- 再次调用 `func` 函数时，静态局部变量 `b` 保持其值，因此 `b` 的值变为 `10 + 5 = 15`。

{'23074':'A'}

------

### 题目23075
问题：36.  改写程序，在第6行和第7行之间加入一行代码，static int n; ，则执行完第13行func函数后，该函数中n的值为：
A. 0
B. 1
C. 12
D. 13


**解析如下：**

------

解析：
- 如果在第6行和第7行之间加入一行代码 `static int n;`，则 `n` 成为静态局部变量。
- 静态局部变量 `n` 在 `main` 函数中初始化为 `0`。
- 1 + 12 = 13, func函数中操作的n是全局变量n，与main函数中的静态变量n无关

{'23075':'D'}

------

### 题目23076
问题：37.  改写程序，在第6行和第7行之间加入一行代码，static int n; ，则执行完第17行后n的值为：
A. 23
B. 10
C. 12
D. 13


**解析如下：**

------

解析：
- 如果在第6行和第7行之间加入一行代码 `static int n;`，则 `n` 成为静态局部变量。
- 静态局部变量 `n` 在 `main` 函数中初始化为 `0`。
- 17行的n是main函数中的静态变量n，优先使用近距离的n。
- n = 10

{'23076':'B'}

------

### 题目23077
问题：38.  改写程序，在第6行和第7行之间加入一行代码，static int n; ，则执行完第18行func函数后，该func函数中n的值为：
A. 35
B. 25
C. 12
D. 13


**解析如下：**

------

解析：
func函数中的n是全局变量n，n = 1 + 12(第一次调用) + 12(第二次调用) = 25

{'23077':'B'}

------

### 题目23078
问题：39.  改写程序，在第6行和第7行之间，21行和22行之间都加入一行代码，static int n; ，则执行完第17行后n的值为：
A. 23
B. 10
C. 12
D. 13


**解析如下：**

------

解析：
- 如果在第6行和第7行之间以及第21行和第22行之间都加入一行代码 `static int n;`，则 `n` 成为静态局部变量。
- 它在func中只会执行一次`static int n;`，然后作用域还是在func中，但是生命周期是直到程序结束。
- 而17行的n是全局作用域n，所以 n = 1 + 10 = 11; 但系统答案给的B

{'23078':'B'}

------

### 题目23079
问题：40.  改写程序，在第6行和第7行之间，21行和22行之间都加入一行代码，static int n; ，则执行完第18行func函数后，该函数中n的值为：
A. 24
B. 23
C. 25
D. 12


**解析如下：**

------

解析：
- 如果在第6行和第7行之间以及第21行和第22行之间都加入一行代码 `static int n;`，则 `n` 成为静态局部变量。
- 静态局部变量 `n` 在 `main` 函数中初始化为 `0`。
- 第25行代码 `n += 12;` 将 `n` 的值增加 `12`，因此 `n` 的值变为 `0 + 12 = 12`。
- 第17行代码 `n += 10;` 将 `n` 的值增加 `10`，因此 `n` 的值变为 `12 + 10 = 22`。
- 再次调用 `func` 函数时，第25行代码 `n += 12;` 将 `n` 的值增加 `12`，因此 `n` 的值变为 `22 + 12 = 34`。

{'23079':'A'}

------

