## ch008_003
``` c++
1)  #include<iostream>
2)  using namespace std;
3)  int sum(int[], int);
4)  void main(){
5)    static int ia[5] = { 2, 3, 6, 8, 10 };
6)    int sumOfArray;
7)    sumOfArray = sum(ia, 5);
8)    cout << "sum of array:" << sumOfArray << endl;
9)    system("pause");
10)  }
11)  int sum(int array[], int len)
12)  {
13)    int iSum = 0;
14)    for (int i = 0; i<len; i++)
15)    iSum += array[i];
16)   return iSum;
17)  }

```
### 题目23727
问题：程序执行完第5行时，数组ia所获得的空间在哪个区域？

A、栈区
B、堆区
C、全局区
D、随机分配


**解析如下：**

------

解析：
在C++中，静态数组（如第5行定义的`static int ia[5]`）在程序执行时会被分配在全局区（也称为静态存储区）。这是因为`static`关键字表示该变量具有静态存储期，即在程序的整个生命周期内都存在，并且存储在全局区。

{'23727':'C'}

------

### 题目23728
问题：程序执行完第6行时，变量sumOfArray所获得的空间在哪个区域？

A、栈区
B、堆区
C、全局区
D、随机分配


**解析如下：**

------

解析：
在C++中，局部变量（如第6行定义的`int sumOfArray`）在程序执行时会被分配在栈区。这是因为局部变量在函数内部定义，具有自动存储期，即在函数调用时分配空间，在函数返回时释放空间。

{'23728':'A'}

------

### 题目23729
问题：程序执行到第7行时，逐语句下一步执行哪一行？

A、8
B、7
C、9
D、12


**解析如下：**

------

解析：
在程序执行到第7行时，`sumOfArray = sum(ia, 5);`这一行代码表示调用`sum`函数，并将返回值赋给`sumOfArray`。因此，逐语句下一步会跳转到`sum`函数的定义处，即第12行。

{'23729':'D'}

------

### 题目23730
问题：程序执行到第12行时，若数组ia的首地址为0x00269000， 第11行中数组array的首地址为多少？

A、0x00269000
B、随机值
C、2
D、0x00269004


**解析如下：**

------

解析：
在C++中，函数参数传递数组时，实际上传递的是数组的首地址。因此，当程序执行到第12行时，`sum`函数中的`array`参数将指向`ia`数组的首地址，即0x00269000。

{'23730':'A'}

------

### 题目23731
问题：程序执行到第12行时，若数组ia的首地址为0x00269000，第11行中变量len的值为多少？

A、0
B、随机值
C、5
D、0x00269000


**解析如下：**

------

解析：
在程序执行到第12行时，`sum`函数的参数`len`的值是由调用`sum`函数时传递的值决定的。在第7行中，`sum(ia, 5)`传递了`5`作为`len`的值。因此，当程序执行到第12行时，`len`的值为5。

{'23731':'C'}

------

### 题目23732
问题：程序第一次执行完第14行时，变量i的值为多少？

A、0
B、随机值
C、1
D、2


**解析如下：**

------

解析：
在程序执行到第14行时，`for`循环的初始化部分`int i = 0;`已经执行，因此变量`i`的初始值为0。第一次执行完第14行时，`i`的值仍然是0，因为此时循环体还没有执行。

{'23732':'A'}

------

### 题目23733
问题：程序第一次执行完第14行时,array[i]为多少？

A、0
B、随机值
C、1
D、2


**解析如下：**

------

解析：
在程序第一次执行完第14行时，`i`的值为0，因此`array[i]`实际上是`array[0]`。根据第5行的数组初始化`static int ia[5] = { 2, 3, 6, 8, 10 };`，`array[0]`的值为2。

{'23733':'D'}

------

### 题目23734
问题：程序第二次执行完第14行时，变量i的值为多少？

A、0
B、随机值
C、1
D、2


**解析如下：**

------

解析：
在程序第二次执行完第14行时，`for`循环已经执行了一次，`i`的值在第一次循环结束时已经递增为1。因此，第二次执行完第14行时，`i`的值为1。

{'23734':'C'}

------

### 题目23735
问题：程序第二次执行完第14行时, array[i]为多少？

A、0
B、3
C、1
D、2


**解析如下：**

------

解析：
在程序第二次执行完第14行时，`i`的值为1，因此`array[i]`实际上是`array[1]`。根据第5行的数组初始化`static int ia[5] = { 2, 3, 6, 8, 10 };`，`array[1]`的值为3。

{'23735':'B'}

------

### 题目23736
问题：程序执行到第16行时,iSum的值为多少？

A、29
B、3
C、1
D、2


**解析如下：**

------

解析：
在程序执行到第16行时，`for`循环已经完成，`iSum`的值是数组`ia`中所有元素的累加和。根据第5行的数组初始化`static int ia[5] = { 2, 3, 6, 8, 10 };`，数组元素的累加和为2 + 3 + 6 + 8 + 10 = 29。

{'23736':'A'}

------

### 题目23737
问题：程序执行到第17行时,下一句执行哪一行？

A、7
B、8
C、9
D、12


**解析如下：**

------

解析：
在程序执行到第17行时，`sum`函数已经执行完毕并返回了`iSum`的值。此时，程序将返回到调用`sum`函数的地方，即第7行`sumOfArray = sum(ia, 5);`进行赋值。

{'23737':'A'}

------

### 题目23738
问题：程序第二次执行完第7行后，sumOfArray的值为多少？

A、5
B、2
C、29
D、12


**解析如下：**

------

解析：
在程序第二次执行完第7行时，`sumOfArray`的值是由`sum`函数返回的值决定的。根据第16行的计算，`sum`函数返回的值是数组`ia`中所有元素的累加和，即29。因此，第二次执行完第7行后，`sumOfArray`的值为29。

{'23738':'C'}

------

### 题目23739
问题：程序第8行执行后，屏幕上输出结果为？

A、sum of array:29
B、“sum of array:” 29
C、29
D、其他


**解析如下：**

------

解析：
在程序第8行执行后，`cout << "sum of array:" << sumOfArray << endl;`将输出字符串`"sum of array:"`，然后输出`sumOfArray`的值，最后输出换行符。根据第7行的计算，`sumOfArray`的值为29。因此，屏幕上输出的结果为`sum of array:29`。

{'23739':'A'}

------

