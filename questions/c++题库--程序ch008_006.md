## ch008_006
``` c++
1)  #include<iostream>
2)  using namespace std;
3)  void main()
4)  {
5)    char line[30] = "I love programing.";
6)    cout << "数组元素个数：" << sizeof(line) << endl;
7)    cout << "字符串中的字符个数：" << strlen(line) << endl;
8)    cout << line << endl;
9)    line[6] = '\0';//增加结束标志
10)    cout << line << endl;
11)    cout << "数组元素个数：" << sizeof(line) << endl;
12)    cout << "字符串中的字符个数：" << strlen(line) << endl;
13)  }

```
### 题目23792
问题：程序第5行中，数组line字符串中的字符个数为：

A、18
B、19
C、20
D、21


**解析如下：**

------

解析：
在第5行中，数组 `line` 被初始化为字符串 `"I love programing."`。这个字符串包含18个字符（包括空格），但不包括字符串结束符 `\0`。因此，字符串中的字符个数为18。

{'23792':'A'}

------

### 题目23793
问题：程序第几行的语句使用了其他的命名空间：

A、第1行
B、第2行
C、第3行
D、第4行


**解析如下：**

------

解析：
在C++中，`using namespace std;` 语句用于引入标准命名空间 `std`，以便在代码中可以直接使用标准库中的名称，而不需要每次都加上 `std::` 前缀。这个语句出现在第2行。

{'23793':'B'}

------

### 题目23794
问题：程序第5行中，数组line系统分配几个字节：

A、27
B、28
C、29
D、30


**解析如下：**

------

解析：
在第5行中，数组 `line` 被定义为 `char line[30]`，这意味着系统为这个数组分配了30个字节的内存空间。每个 `char` 类型占用1个字节，因此30个 `char` 类型总共占用30个字节。

{'23794':'D'}

------

### 题目23795
问题：数组line中第19到第30个数组元素中的值是什么：

A、.
B、’\0’
C、乱码
D、随机值


**解析如下：**

------

解析：
题库提示：字符数组中没有进行初始化的字符为’\0’

{'23795':'B'}

------

### 题目23796
问题：程序第3行的main（）函数返回值类型：

A、整型
B、浮点型
C、字符型
D、无返回值


**解析如下：**

------

解析：
在C++中，`main()` 函数的返回值类型通常是 `int`，表示程序的退出状态。然而，在代码中，`main()` 函数被声明为 `void`，这意味着它没有返回值。

{'23796':'D'}

------

### 题目23797
问题：程序第6行和第11行输出的数组元素个数分别是：

A、30，6
B、18，6
C、30，30
D、18，30


**解析如下：**

------

解析：
在第6行和第11行中，`sizeof(line)` 用于计算数组 `line` 的总大小（以字节为单位）。由于 `line` 是一个 `char` 类型的数组，定义为 `char line[30]`，因此 `sizeof(line)` 的结果始终是30，无论数组中的内容如何变化。

{'23797':'C'}

------

### 题目23798
问题：程序第9行中，增加结束标志后的line数组的元素个数：

A、30
B、31
C、32
D、33


**解析如下：**

------

解析：
在第9行中，`line[6] = '\0';` 语句将数组的第7个元素（索引为6）设置为字符串结束符 `\0`。然而，这并不会改变数组 `line` 的总大小。数组 `line` 仍然是一个 `char` 类型的数组，定义为 `char line[30]`，因此它的元素个数仍然是30。

{'23798':'A'}

------

### 题目23799
问题：程序第9行中，增加结束标志后的line字符串的字符个数：

A、4
B、5
C、6
D、7


**解析如下：**

------

解析：
在第9行中，`line[6] = '\0';` 语句将数组的第7个元素（索引为6）设置为字符串结束符 `\0`。这意味着字符串 `"I love programing."` 在第7个字符处被截断，因此有效的字符个数变为6（包括空格）。

{'23799':'C'}

------

### 题目23800
问题：在程序第9行后，添加语句cout<<line[6]<<endl;，则会输出的是：

A、p
B、programing.
C、输出空白
D、程序出错


**解析如下：**

------

解析：
在第9行中，`line[6] = '\0';` 语句将数组的第7个元素（索引为6）设置为字符串结束符 `\0`。因此，`line[6]` 的值现在是 `\0`，输出时会显示为空白。

{'23800':'C'}

------

### 题目23801
问题：在程序第9行后，添加语句cout<<line[7]<<endl;，则会输出的是：

A、p
B、programing.
C、输出空白
D、程序出错


**解析如下：**

------

解析：
数一下个数即可

{'23801':'A'}

------

### 题目23802
问题：在程序第9行后，添加语句cout<<line+7<<endl;，则会输出的是：

A、p
B、programing.
C、输出空白
D、程序出错


**解析如下：**

------

解析：
在第9行中，`line[6] = '\0';` 语句将数组的第7个元素（索引为6）设置为字符串结束符 `\0`。这意味着字符串在第7个字符处被截断。添加语句 `cout << line + 7 << endl;` 会输出从 `line[7]` 开始的字符串。

{'23802':'B'}

------

### 题目23803
问题：在程序第5行后添加char *ptr=line;语句,则以下说法正确的是： 

A、ptr的值I
B、在该语句后执行cout<<ptr<<endl;,则输出I
C、在该语句后执行cout<<*ptr<<endl;,则输出I love programing.
D、ptr指向的是line数组的首地址


**解析如下：**

------

解析：
在第5行后添加 `char *ptr = line;` 语句，`ptr` 是一个指向 `line` 数组首地址的指针。因此，`ptr` 指向的是 `line` 数组的首地址。执行 `cout << ptr << endl;` 会输出从 `line` 数组首地址开始的字符串，即 `"I love programing."`。执行 `cout << *ptr << endl;` 会输出 `ptr` 指向的字符，即 `'I'`。

{'23803':'D'}

------

### 题目23804
问题：程序的第6行中的cout表示：

A、显示字符串
B、标准输出的流设备
C、标准输入的流设备
D、程序入口


**解析如下：**

------

解析：
在C++中，`cout` 是标准输出流对象，用于向标准输出设备（通常是控制台）输出数据。因此，程序的第6行中的 `cout` 表示标准输出的流设备。

{'23804':'B'}

------

### 题目23805
问题：该程序第6行中哪一个头文件涉及cout对象？

A、iostream
B、cmath
C、iomanip
D、stdlib


**解析如下：**

------

解析：
`cout` 对象是C++标准库中的标准输出流对象，定义在 `<iostream>` 头文件中。因此，涉及 `cout` 对象的头文件是 `<iostream>`。

{'23805':'A'}

------

### 题目23806
问题：结束标志字符’\\0’的ASCII码值是：

A、0
B、67
C、100
D、127


**解析如下：**

------

解析：
结束标志字符 `'\0'` 是字符串的结束符，其ASCII码值为0。

{'23806':'A'}

------

