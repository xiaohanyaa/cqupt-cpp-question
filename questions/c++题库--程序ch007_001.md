## ch007_001
``` c++
1)  //**********************
2)  //** ch07_01（共30题）**
3)  //**********************
4)  #include<iostream>
5)  using namespace std;
6)  void main(){
7)    static int a[] = { 1, 2, 4, 8, 16 };
8)    cout << "内存大小：" << sizeof(a) << endl;
9)    cout << "机器字长：" << sizeof(int) << endl;
10)    cout << "元素个数：" << sizeof(a) / sizeof(int) << endl;
11)    system("pause");
12)  }

```
### 题目23164
问题：1.  程序第7行的作用是：
A.  定义一个整型变量
B.  定义一个静态整型变量
C.  定义一个长度为5的整型数组
D.  定义一个至少包含5个整数的数组


**解析如下：**

------

解析：
程序第7行的作用是定义一个静态整型数组，数组名为`a`，包含5个元素，分别是1, 2, 4, 8, 16。`static`关键字表示该数组是静态的，即在程序运行期间一直存在。

{'23164':'C'}

------

### 题目23257
问题：9. 数组a的数据元素个数是多少？
A.  1
B.  4
C.  不确定
D.  5


**解析如下：**

------

解析：
根据程序第7行的定义，数组`a`包含5个元素，分别是1, 2, 4, 8, 16。因此，数组`a`的数据元素个数是5。

{'23257':'D'}

------

### 题目23258
问题：8. 程序执行完第7行之后，a[5]的值是？
A.  0
B.  1
C.  16
D.  不存在


**解析如下：**

------

解析：
程序第7行定义了一个包含5个元素的数组`a`，分别是1, 2, 4, 8, 16。数组的下标从0开始，因此`a[5]`超出了数组的范围，是无效的访问。在C++中，访问超出数组范围的元素会导致未定义行为。

{'23258':'D'}

------

### 题目23259
问题：7. 程序执行完第7行之后，a[4]的值是？
A.  1
B.  4
C.  8
D.  16


**解析如下：**

------

解析：
程序第7行定义了一个包含5个元素的数组`a`，分别是1, 2, 4, 8, 16。数组的下标从0开始，因此`a[4]`是数组的最后一个元素，其值为16。

{'23259':'D'}

------

### 题目23260
问题：6. 程序执行完第7行之后，a[3]的值是？
A.  1
B.  2
C.  4
D.  8


**解析如下：**

------

解析：
程序第7行定义了一个包含5个元素的数组`a`，分别是1, 2, 4, 8, 16。数组的下标从0开始，因此`a[3]`是数组的第四个元素，其值为8。

{'23260':'D'}

------

### 题目23261
问题：5. 程序执行完第7行之后，a[2]的值是？
A.  1
B.  2
C.  4
D.  8


**解析如下：**

------

解析：
程序第7行定义了一个包含5个元素的数组`a`，分别是1, 2, 4, 8, 16。数组的下标从0开始，因此`a[2]`是数组的第三个元素，其值为4。

{'23261':'C'}

------

### 题目23262
问题：4. 程序执行完第7行之后，a[1]的值是？
A.  1
B.  2
C.  4
D.  8


**解析如下：**

------

解析：
程序第7行定义了一个包含5个元素的数组`a`，分别是1, 2, 4, 8, 16。数组的下标从0开始，因此`a[1]`是数组的第二个元素，其值为2。

{'23262':'B'}

------

### 题目23263
问题：3. 程序执行完第7行之后，a[0]的值是？
A.  1
B.  2
C.  4
D.  8


**解析如下：**

------

解析：
程序第7行定义了一个包含5个元素的数组`a`，分别是1, 2, 4, 8, 16。数组的下标从0开始，因此`a[0]`是数组的第一个元素，其值为1。

{'23263':'A'}

------

### 题目23264
问题：29.若在第7行和第8行中间增加一条语句inta=0;重新构建程序，会出现什么问题？
A.  未定义的标识符a
B.  类型不匹配
C.  连接错误
D.  没有问题


**解析如下：**

------

解析：
在第7行和第8行中间增加一条语句`int a = 0;`会导致变量`a`的重复定义。因为第7行已经定义了一个名为`a`的数组，再次定义一个同名的整型变量会导致编译错误，提示类型不匹配。

{'23264':'B'}

------

### 题目23265
问题：28.若要使数组a的数据元素个数不变，初始值全部为0，下列选项中哪个不正确？
A.  static int a[]={0,0,0,0,0};
B.  static int a[]={0};
C.  static int a[5]={0};
D.  static int a[5]={0,0,0,0,0};


**解析如下：**

------

解析：
选项B中的`static int a[] = {0};`只初始化了一个元素为0，而数组`a`的长度为5，因此数组的其他元素将是未初始化的，这不符合题目要求。其他选项都正确地初始化了数组的所有元素为0。

{'23265':'B'}

------

### 题目23266
问题：27.若要使数组a的数据元素个数不变，初始值全部为1，下列选项中哪个正确？
A.  static int a[]={1}；
B.  static int a[5]={1};
C.  static int a[]={1,1,1,1,1};
D.  static int a[5]={1,,,,1};


**解析如下：**

------

解析：
选项C中的`static int a[] = {1, 1, 1, 1, 1};`正确地初始化了数组的所有5个元素为1。其他选项要么没有完全初始化所有元素，要么语法不正确。

{'23266':'C'}

------

### 题目23267
问题：26.若将程序第7行修改为static int a[]={1,2,4,,16};则程序会出现什么问题？
A.  初始化数据过少
B.  初始化数据过多
C.  语法错误
D.  缺少数组大小


**解析如下：**

------

解析：
将程序第7行修改为`static int a[] = {1, 2, 4, , 16};`会导致语法错误，因为在初始化列表中不能有空元素。正确的初始化列表应该包含所有元素的值。

{'23267':'C'}

------

### 题目23268
问题：25.若将程序第7行修改为static int a[4]={1,2,4,8,16};则程序会出现什么问题？
A.  编译错误：数组太小
B.  编译错误：初始化参数过多
C.  编译错误：数据类型不匹配
D.  编译错误：不能对数组进行初始化


**解析如下：**

------

解析：
将程序第7行修改为`static int a[4] = {1, 2, 4, 8, 16};`会导致编译错误，因为数组的大小为4，但初始化列表中有5个元素，初始化参数过多。

{'23268':'B'}

------

### 题目23269
问题：24.若将程序第7行修改为static int a[10]={1,2,4,8,16};则a中后5个数据元素的初始值是多少？
A.  0
B.  1
C.  16
D.  随机值


**解析如下：**

------

解析：
将程序第7行修改为`static int a[10] = {1, 2, 4, 8, 16};`后，数组`a`的大小为10，但只初始化了前5个元素。未显式初始化的元素将自动初始化为0。因此，数组`a`中后5个数据元素的初始值是0。

{'23269':'A'}

------

### 题目23270
问题：23.若将程序第7行修改为static int a[10]={1,2,4,8,16};则a中共多少个数据元素？
A.  5个
B.  6个
C.  10个
D.  11个


**解析如下：**

------

解析：
将程序第7行修改为`static int a[10] = {1, 2, 4, 8, 16};`后，数组`a`的大小为10，因此数组`a`中共有10个数据元素。

{'23270':'C'}

------

### 题目23271
问题：22.程序第7行中[]省略的值是多少？
A.  0
B.  1
C.  5
D.  6


**解析如下：**

------

解析：
程序第7行中`static int a[] = {1, 2, 4, 8, 16};`省略了数组的大小，编译器会根据初始化列表中的元素个数自动确定数组的大小。初始化列表中有5个元素，因此数组`a`的大小为5。

{'23271':'C'}

------

### 题目23272
问题：21.程序第7行中a是什么？
A.  变量名
B.  数组名
C.  函数名
D.  一个字符


**解析如下：**

------

解析：
程序第7行中`static int a[] = {1, 2, 4, 8, 16};`定义了一个名为`a`的数组，因此`a`是数组名。

{'23272':'B'}

------

### 题目23273
问题：20.程序第10行共有多少个运算符？
A.  1个
B.  2个
C.  3个
D.  6个


**解析如下：**

------

解析：
题库提示：3个输出流运算符，1个除法运算符，2个sizeof运算符。
注意，这是是个数，而不是种数！！！

{'23273':'D'}

------

### 题目23274
问题：2. 数组a存储在哪里？
A.  主函数的栈空间
B.  全局数据区
C.  堆空间
D.  代码区


**解析如下：**

------

解析：
数组`a`在程序第7行被定义为`static int a[] = {1, 2, 4, 8, 16};`，`static`关键字表示该数组是静态的，存储在全局数据区。

{'23274':'B'}

------

### 题目23275
问题：19.程序第7行共有多少个运算符？
A.  2个
B.  3个
C.  5个
D.  6个


**解析如下：**

------

解析：
题库提示:1个下标运算符、1个赋值运算符、4个逗号运算符。

{'23275':'D'}

------

### 题目23276
问题：18.程序第7行若修改成static int a[4]={1,2,4,8,16},编译时会报什么？
A.  编译通过
B.  编译错误，静态数组不能初始化
C.  编译错误，初始值过多
D.  编译错误，类型不匹配


**解析如下：**

------

解析：
将程序第7行修改为`static int a[4] = {1, 2, 4, 8, 16};`会导致编译错误，因为数组的大小为4，但初始化列表中有5个元素，初始值过多。

{'23276':'C'}

------

### 题目23277
问题：17.与程序第5行无关的是：
A.  第8行
B.  第9行
C.  第10行
D.  第11行


**解析如下：**

------

解析：
程序第5行`using namespace std;`引入了标准命名空间，使得后续代码可以直接使用标准库中的函数和对象，如`cout`和`endl`。第8行、第9行和第10行都使用了标准库中的`cout`和`endl`，而第11行使用了`system("pause");`，与标准命名空间无关。

{'23277':'D'}

------

### 题目23278
问题：16.与程序第4行有关的是：
A.  第6行
B.  第7行
C.  第8行
D.  第11行


**解析如下：**

------

解析：
程序第4行`#include <iostream>`包含了标准输入输出库，使得后续代码可以使用`cout`和`endl`等标准库中的函数和对象。第8行使用了`cout`，因此与第4行有关。

{'23278':'C'}

------

### 题目23279
问题：15.以下对数组a的描述不正确的是：
A.  a有5个数据元素
B.  a的初始值分别为1,2,4,8,16
C.  a存储在全局数据区中
D.  a中数据元素的值在程序运行期间不可以被修改


**解析如下：**

------

解析：
数组`a`在程序第7行被定义为`static int a[] = {1, 2, 4, 8, 16};`，`static`关键字表示该数组是静态的，存储在全局数据区。数组`a`有5个数据元素，初始值分别为1, 2, 4, 8, 16。数组`a`中的数据元素在程序运行期间可以被修改。

{'23279':'D'}

------

### 题目23280
问题：14.数组a的存储空间何时分配？
A.  程序执行到第6行时
B.  程序执行到第7行时
C.  主函数运行前
D.  编译期间


**解析如下：**

------

解析：
题库提示：**全局数据**和**静态数据**在**编译期间分配存储空间**。

{'23280':'D'}

------

### 题目23281
问题：13.程序执行完第10行，控制台窗口第三行显示：
A.  元素个数：1
B.  元素个数：4
C.  元素个数：5
D.  元素个数：20


**解析如下：**

------

解析：
程序第10行`cout << "元素个数：" << sizeof(a) / sizeof(int) << endl;`计算数组`a`的元素个数。数组`a`的大小为20字节（5个元素，每个元素4字节），因此`sizeof(a) / sizeof(int)`的结果为5。

{'23281':'C'}

------

### 题目23282
问题：12.在32位系统上，程序执行完第9行，控制台窗口第二行显示：
A.  机器字长：4
B.  机器字长：1
C.  机器字长：2
D.  机器字长：20


**解析如下：**

------

解析：
在32位系统上，`sizeof(int)`的结果为4字节。程序第9行`cout << "机器字长：" << sizeof(int) << endl;`将输出`机器字长：4`。

{'23282':'A'}

------

### 题目23283
问题：11.在32位系统上，程序执行完第8行，控制台窗口显示：
A.  内存大小：20
B.  内存大小：4
C.  内存大小：16
D.  内存大小：5


**解析如下：**

------

解析：
在32位系统上，数组`a`的大小为20字节（5个元素，每个元素4字节）。程序第8行`cout << "内存大小：" << sizeof(a) << endl;`将输出`内存大小：20`。

{'23283':'A'}

------

### 题目23284
问题：10.数组a的第一个数据元素下标是几？
A.  1
B.  0
C.  不确定
D.  -1


**解析如下：**

------

解析：
数组`a`的第一个数据元素下标是0。在C++中，数组的下标从0开始。

{'23284':'B'}

------

### 题目23285
问题：1. 程序第7行的static的含义是？
A.  外部的
B.  动态的
C.  全局的
D.  静态的


**解析如下：**

------

解析：
程序第7行`static int a[] = {1, 2, 4, 8, 16};`中的`static`关键字表示该数组是静态的，即在程序运行期间一直存在，存储在全局数据区。

{'23285':'D'}

------

