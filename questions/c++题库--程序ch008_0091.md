## ch008_0091
``` c++
1)  #include <iostream>
2)  using namespace std;
3)  void strCopy(char* dest, char* source)
4)  {
5)    while (*dest++ = *source++);
6)  }
7)  void strAdd(char* dest, char* source)
8)  {
9)    while (*dest){  //移到结束标志
10)      dest++;
11)    }
12)    while (*source){
13)      *dest = *source;//复制当前字符
14)      dest++;
15)      source++;
16)    };
17)    *dest = '\0';//设置结束标志
18)  }
19)  void main(){
20)    char a[20] = "copy string.";
21)    //字符串加法
22)    char c[30] = "computer ";
23)    strAdd(c, a);
24)    cout << "连接后的字符串长度:" << strlen(c) + strlen("1234567890123") << endl;
25)    strAdd(c, "1234567890123");
26)    cout << c << endl;
27)    system("pause");
28)  }

```
### 题目24140
问题：1.  删除该程序的3-6行，下列描述正确的是（    ）：
A.  程序编译无法通过。
B.  程序编译可以通过，链接时报错。
C.  程序编译可以通过并能够运行。
D.  无法判断。


**解析如下：**

------

解析：
删除程序的3-6行后，程序中仍然存在`strAdd`函数，该函数的功能是将两个字符串连接起来。虽然`strCopy`函数被删除了，但程序中并没有调用`strCopy`函数，因此删除`strCopy`函数不会影响程序的编译和运行。程序仍然可以编译通过并正常运行，输出连接后的字符串及其长度。

{'24140':'C'}

------

### 题目24141
问题：2.  程序第24行执行后，屏幕输出结果为(  )：
A.  连接后的字符串长度:34
B.  连接后的字符串长度:331234567890123
C.  连接后的字符串长度:33
D.  连接后的字符串长度:1234567890123


**解析如下：**

------

解析：
程序第24行执行后，`c`字符串的内容为`"computer copy string."`，其长度为18。然后计算`strlen(c) + strlen("1234567890123")`，即21 + 13 = 31。因此，屏幕输出结果为`连接后的字符串长度:34`。

{'24141':'A'}

------

### 题目24142
问题：3.  程序第20行执行后，a[10]的值为(  )：
A.  ‘n’
B.  ‘g’
C.  ‘\0’
D.  ‘.’


**解析如下：**

------

解析：
程序第20行执行后，`a`字符串的内容为`"copy string."`。根据字符串的定义，`a[10]`是字符串的结束标志`g`。

{'24142':'B'}

------

### 题目24143
问题：4.  程序第20行执行后，a[13]的值为(  )：
A.  ‘0’
B.  随机值
C.  ‘\0’
D.  ‘.’


**解析如下：**

------

解析：
题库提示：a数组在初始化时，使用“copy string.”进行初始化，只可以初始化12个元素，其余元素的值被初始化为‘\0’.‘\0’和‘0’是不同的值。

{'24143':'C'}

------

### 题目24144
问题：5.  程序第23行执行后，数组c存储的内容为(  )：
A.  “copy string.”
B.  “computer”
C.  “copy string.computer ”
D.  “computer copy string.”


**解析如下：**

------

解析：
程序第23行执行后，`strAdd`函数将`a`字符串的内容`"copy string."`连接到`c`字符串的末尾。`c`字符串的初始内容为`"computer "`，连接后的内容为`"computer copy string."`。

{'24144':'D'}

------

### 题目24145
问题：6.  程序第25行执行后，数组元素c[24]的值为(  )：
A.  ‘3’
B.  ‘4’
C.  ‘5’
D.  ‘\\0’


**解析如下：**

------

解析：
程序第25行执行后，`strAdd`函数将字符串`"1234567890123"`连接到`c`字符串的末尾。`c`字符串的初始内容为`"computer copy string."`，连接后的内容为`"computer copy string.1234567890123"`。`c[24]`对应的是`'4'`。

{'24145':'B'}

------

### 题目24146
问题：7.  函数strAdd()第一次被调用时，当程序执行到第12行时，*dest的值为(  )：
A.  ‘c’
B.  ‘ ’
C.  ‘0’
D.  ‘\0’


**解析如下：**

------

解析：
函数`strAdd()`第一次被调用时，`dest`指向的是`c`字符串的第一个字符。`c`字符串的初始内容为`"computer "`，执行到'\0'处跳出循环。

{'24146':'D'}

------

### 题目24147
问题：8.  函数strAdd()第一次被调用时，当程序执行到第13行时，*source的值为(  )：
A.  ‘c’
B.  ‘ ’
C.  ‘0’
D.  ‘\\0’


**解析如下：**

------

解析：
函数`strAdd()`第一次被调用时，`source`指向的是`a`字符串的第一个字符。`a`字符串的内容为`"copy string."`，因此当程序执行到第13行时，`*source`的值为`'c'`。

{'24147':'A'}

------

### 题目24148
问题：9.  函数strAdd()第一次被调用时，当程序执行完第14行后，*dest的值为(  )：
A.  ‘c’
B.  ‘ ’
C.  ‘0’
D.  ‘\0’


**解析如下：**

------

解析：
函数`strAdd()`第一次被调用时，当程序执行完第14行后，`*dest++`后，是下一个`\0`。

{'24148':'D'}

------

### 题目24149
问题：10.  函数strAdd()第一次被调用时，当程序执行完第13行后，如果立即执行语句“cout<<c<<endl；”，则屏幕输出结果为(  )：
A.  computer copy string.
B.  computer c
C.  computer copy 
D.  computer 


**解析如下：**

------

解析：
函数`strAdd()`第一次被调用时，当程序执行完第13行后，`c`字符串的内容为`"computer c"`，因为此时`dest`指向的是`c`字符串的下一个字符，并且已经将`source`的第一个字符`'c'`复制到了`dest`指向的位置。

{'24149':'B'}

------

### 题目24150
问题：11.  如果把程序第12行~16行，使用语句“while (*dest++ = *source++);”替换，则下述描述正确的是(  )：
A.  替换后的程序代码，无法实现原有功能。
B.  替换后的程序代码，在编译时出错。
C.  替换后的程序代码，可以实现原有功能。 
D.  以上描述都有可能。


**解析如下：**

------

解析：
如果把程序第12行~16行替换为`while (*dest++ = *source++);`，该语句的功能是将`source`字符串的内容复制到`dest`字符串中，直到遇到`source`字符串的结束标志`'\0'`。这与原代码的功能相同，因此替换后的程序代码可以实现原有功能。

{'24150':'C'}

------

### 题目24151
问题：12.  如果把程序第12行~16行，与下述代码等价的是(  )：
A.  “while (*(dest++) = *(source++));”。
B.  “while (*dest++ == *source++);”。
C.  “while (*dest++ = *source++)”。 
D.  “while ((*dest)++ = (*source)++);”。 


**解析如下：**

------

解析：
题库提示：程序第12行~16行与语句“while (*dest++ = *source++);”相同，可以替换，*dest++中，如果认为*和++优先级相同，则从结合性从右到左来看，可以理解为*(dest++)。如果认为++优先级比*高(书上是这样的)，也可以理解为*(dest++)。
程序第12行~16行的功能是将`source`字符串的内容复制到`dest`字符串中，直到遇到`source`字符串的结束标志`'\0'`。等价的代码是`while (*dest++ = *source++);`，这与选项C完全一致。
可以认为C也是正确的。

{'24151':'A'}

------

### 题目24152
问题：13.  如下与程序第5行代码等价的是(  )：
A.  “while (*dest++ == *source++);”
B.  “while ((*dest)++ == *source++)”
C.  “while ((*dest)++ = (*source)++);” 
D.  “while (*(dest++) = *(source++));”


**解析如下：**

------

解析：
程序第5行代码的功能是将`source`字符串的内容复制到`dest`字符串中，直到遇到`source`字符串的结束标志`'\0'`。等价的代码是`while (*(dest++) = *(source++));`，这与选项D完全一致。

{'24152':'D'}

------

### 题目24153
问题：14.  关于表达式“*dest++ = *source++”，如下描述最正确的是(  )：
A.  表达式的值是*dest 。
B.  表达式的值是dest
C.  表达式的值是source
D.  表达式的值是*source


**解析如下：**

------

解析：
题库提示：该表达式是一个赋值操作，其值为赋值符左边的值*dest++，其值为*(dest++)，即*dest。

{'24153':'A'}

------

### 题目24154
问题：15.  如果把第7行代码修改为“void strAdd(const char* dest, const char* source)”，如下描述最正确的是(  )：
A.  程序编译无法通过。
B.  程序编译可以通过，链接时报错。
C.  程序编译可以通过并能够运行。
D.  无法判断。


**解析如下：**

------

解析：
如果把第7行代码修改为`void strAdd(const char* dest, const char* source)`，这意味着`dest`和`source`指针指向的内容是常量，不能被修改。然而，在函数内部，`dest`指针指向的内容被修改（例如第13行和第14行），这会导致编译错误，因为不能修改常量。

{'24154':'A'}

------

### 题目24155
问题：16.  如果把第7行代码修改为“void strAdd(char* dest, const char* source)”，如下描述最正确的是(  )：
A.  程序编译无法通过。
B.  程序编译可以通过，链接时报错。
C.  程序编译可以通过并能够运行。
D.  无法判断。


**解析如下：**

------

解析：
如果把第7行代码修改为`void strAdd(char* dest, const char* source)`，这意味着`source`指针指向的内容是常量，不能被修改。然而，在函数内部，`source`指针指向的内容并没有被修改，因此程序可以正常编译并通过，并且能够运行。

{'24155':'C'}

------

### 题目24156
问题：17.  函数strAdd()第2次被调用时，当程序执行完第13行后，如果立即执行语句“cout<<c<<endl；”，则屏幕输出结果为(  )：
A.  computer copy string.
B.  computer c
C.  computer copy string.1 
D.  1 


**解析如下：**

------

解析：
函数`strAdd()`第二次被调用时，`source`指向的是字符串`"1234567890123"`的第一个字符`'1'`。当程序执行完第13行后，`c`字符串的内容为`"computer copy string.1"`，因为此时`dest`指向的是`c`字符串的下一个字符，并且已经将`source`的第一个字符`'1'`复制到了`dest`指向的位置。

{'24156':'C'}

------

### 题目24157
问题：
18.  函数strAdd()第2次被调用时，当程序执行完第14行后，*dest的值为(  )：
A.  ‘c’
B.  ‘ ’
C.  ‘0’
D.  ‘\0’


**解析如下：**

------

解析：
函数`strAdd()`第二次被调用时，当程序执行完第14行后，`*dest++`,此时`dest`已经指向了`c`字符串的下一个字符`\0`。

{'24157':'D'}

------

### 题目24158
问题：19.  函数strAdd()第2次被调用时，当程序执行完第13行后，*dest的值为(  )：
A.  ‘c’
B.  ‘1’
C.  ‘ ’
D.  ‘0’


**解析如下：**

------

解析：
函数`strAdd()`第二次被调用时，当程序执行完第13行后，`*dest`的值为`'1'`，因为此时`dest`指向的是`c`字符串的下一个字符，并且已经将`source`的第一个字符`'1'`复制到了`dest`指向的位置。

{'24158':'B'}

------

### 题目24159
问题：20.  逐过程调试语句，程序第26行执行后，在屏幕上输出结果为(  )：
A.  computer copy string.
B.  computer copy string.123456789
C.  computer copy string.1234567890123
D.  运行报错


**解析如下：**

------

解析：
程序第26行执行后，`c`字符串的内容为`"computer copy string.1234567890123"`，因为此时`strAdd`函数已经将字符串`"1234567890123"`连接到了`c`字符串的末尾。

{'24159':'C'}

------

