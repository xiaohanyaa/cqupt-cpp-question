## ch008_005
``` c++
1)  #include<iostream>
2)  using namespace std;
3)  int* createArray(int size){
4)   int *ptr;
5)   ptr = (int*)new int[size];
6)   //初始化数组
7)   for (int i = 0; i<size; i++){
8)    *(ptr + i) = i;
9)    }
10)   return ptr;
11)  }
12)  void printArray(int a[], int len){
13)   for (int i = 0; i<len; i++){
14)    cout << a[i] << ",";
15)   }
16)   cout << endl;
17)  }
18)  void main(){
19)   int row = 4;
20)   int*array;
21)   array = createArray(row);
22)   //可以增加很功能
23)   printArray(array, row);
24)   delete[]array;
25)   system("pause");
26)  }

```
### 题目23778
问题：删去第2行编译时，首先报错的：

A、第3行
B、第14行
C、第16行
D、第25行


**解析如下：**

------

解析：
删去第2行 `using namespace std;` 后，编译器将无法识别 `std::` 命名空间中的 `cout` 和 `endl`。因此，首先报错的行将是使用 `cout` 和 `endl` 的行。在代码中，第14行和第16行都使用了 `cout` 和 `endl`。但是，由于第14行是函数 `printArray` 的一部分，而 `printArray` 函数在第23行被调用，因此编译器会首先在第14行报错，因为它在解析函数定义时就会遇到未定义的 `cout`。

{'23778':'B'}

------

### 题目23779
问题：程序第3行返回值的类型

A、整型
B、指向整型数据的指针
C、浮点型
D、指向浮点型数据的指针


**解析如下：**

------

解析：
程序第3行定义了一个函数 `createArray`，其返回类型是指向整型数据的指针 `int*`。因此，返回值的类型是指向整型数据的指针。

{'23779':'B'}

------

### 题目23780
问题：第4行的指针ptr在内存中的存放位置是：

A、代码区
B、全局数据区
C、堆区
D、栈区


**解析如下：**

------

解析：
第4行声明了一个指针 `ptr`，但它只是一个局部变量，存储在栈区。然而，第5行通过 `new` 操作符在堆区分配了内存，并将堆区内存的地址赋给了 `ptr`。因此，`ptr` 本身存储在栈区，但它指向的内存位于堆区。

{'23780':'D'}

------

### 题目23781
问题：程序第4行的指针ptr在内存中的占用的大小是：

A、1字节
B、0字节
C、8字节
D、4字节


**解析如下：**

------

解析：
程序第4行声明了一个指针 `ptr`，指针的大小取决于系统的架构。在32位系统中，指针通常占用4字节；在64位系统中，指针通常占用8字节。因此，指针 `ptr` 在内存中的占用大小是4字节或8字节。

{'23781':'D'}

------

### 题目23782
问题：程序第5行执行完后ptr数组的元素个数是：

A、1
B、2
C、3
D、4


**解析如下：**

------

解析：
程序第5行通过 `new` 操作符动态分配了一个大小为 `size` 的整型数组，并将分配的内存地址赋给了指针 `ptr`。在第19行，`row` 被赋值为4，因此在第21行调用 `createArray(row)` 时，`size` 的值为4。因此，第5行执行完后，`ptr` 数组的元素个数是4。

{'23782':'D'}

------

### 题目23783
问题：程序第5行执行完后数组中数组元素的值是：

A、全0
B、随机值
C、0到size-1
D、1到size


**解析如下：**

------

解析：
题库提示：动态申请空间后没有进行初始化，所以其中值是随机值。

{'23783':'B'}

------

### 题目23784
问题：程序第9行执行完后数组的值是：

A、全0
B、随机值
C、0到size-1
D、1到size


**解析如下：**

------

解析：
程序第9行执行完后，数组的每个元素都被赋值为其索引值 `i`，即从0到 `size-1`。因此，数组的值是0到 `size-1`。

{'23784':'C'}

------

### 题目23785
问题：程序第14行执行多少次

A、3
B、4
C、5
D、6


**解析如下：**

------

解析：
程序第14行位于 `printArray` 函数中，该函数在第23行被调用，传入的参数 `len` 是 `row`，即4。因此，第14行的循环会执行4次。

{'23785':'B'}

------

### 题目23786
问题：程序第16行的“endl”在控制流输出时起到（ ）作用

A、结束
B、回车
C、换行
D、退格


**解析如下：**

------

解析：
程序第16行的 `endl` 是 C++ 中的一个操纵符，用于在输出流中插入一个换行符，并刷新输出缓冲区。因此，`endl` 在控制流输出时起到换行的作用。

{'23786':'C'}

------

### 题目23787
问题：程序第18行中，main()函数只能有几个参数：

A、不能有参数
B、1
C、2
D、3


**解析如下：**

------

解析：
在 C++ 中，`main()` 函数的标准形式有两种：
1. `int main()`
2. `int main(int argc, char* argv[])`

其中，第一种形式没有参数，第二种形式有两个参数。因此，`main()` 函数只能有0个或2个参数。

{'23787':'C'}

------

### 题目23788
问题：第21行执行完后，若array的值为005D2FC0，则执行完第12行后数组a的首地是：

A、0
B、随机值
C、005D2FC0
D、005D2FC4


**解析如下：**

------

解析：
第21行执行完后，`array` 的值为 `005D2FC0`，即 `array` 指向的内存地址是 `005D2FC0`。第23行调用 `printArray(array, row)`，将 `array` 作为参数传递给 `printArray` 函数。在 `printArray` 函数中，`array` 被当作数组 `a` 的首地址。因此，数组 `a` 的首地址是 `005D2FC0`。

{'23788':'C'}

------

### 题目23789
问题：第21行执行完后，array的值为005D2FC0，则继续执行完第15行后，a[len-1]地址是：

A、0
B、随机值
C、005D2FC0
D、005D2FCC


**解析如下：**

------

解析：
第21行执行完后，`array` 的值为 `005D2FC0`，即 `array` 指向的内存地址是 `005D2FC0`。第23行调用 `printArray(array, row)`，将 `array` 作为参数传递给 `printArray` 函数。在 `printArray` 函数中，`array` 被当作数组 `a` 的首地址。因此，数组 `a` 的首地址是 `005D2FC0`。

第15行输出 `a[len-1]`，其中 `len` 是 `row`，即4。因此，`a[len-1]` 是 `a[3]`。由于 `a` 的首地址是 `005D2FC0`，每个整数占用4字节，所以 `a[3]` 的地址是 `005D2FC0 + 3 * 4 = 005D2FCC`。

{'23789':'D'}

------

### 题目23790
问题：程序执行完第23行后，在逐语句执行中，接着执行哪一行：

A、第11行
B、第12行
C、第24行
D、第25行


**解析如下：**

------

解析：
题库提示：程序执行完第23行后，在逐语句过程调试中，遇到函数调用后，会进入printArray函数体,从大括号开始执行，即执行第12行语句.

程序执行完第23行后，`printArray(array, row)` 函数调用结束，控制流返回到 `main` 函数中。在逐语句执行中，接着执行的是 `main` 函数中的下一行，即第24行。
题目说的是执行完，所以应该是24行，但是题库给的答案是B

{'23790':'B'}

------

### 题目23791
问题：程序执行完第24行的作用是：

A、使整个array数组中各元素为0
B、删除array指针变量
C、释放array所指向地址的连续的空间
D、删除ptr所指向地址的连续空间


**解析如下：**

------

解析：
程序第24行执行 `delete[] array;`，这行代码的作用是释放 `array` 所指向的连续内存空间。`array` 是在第21行通过 `createArray` 函数动态分配的数组，因此 `delete[]` 用于释放这块内存。

{'23791':'C'}

------

