## ch007_007
``` c++
1)  #include<iostream>
2)  #include<iomanip>
3)  using namespace std;
4)  //数组A
5)  const int arow = 3, acol = 4;
6)  int a[arow][acol] = {
7)   { 5, 7, 8, 2 },
8)   { -2, 4, 1, 1 },
9)   { 1, 2, 3, 4 }
10)  };
11)   //数组B
12)   const int brow = 4, bcol = 5;
13)   int b[brow][bcol] = {
14)    { 4, -2, 3, 3, 9 },
15)    { 4, 3, 8, -1, 2 },
16)    { 2, 3, 5, 2, 7 },
17)    { 1, 0, 6, 3, 4 }
18)   };
19)   //数组C
20)   const int crow = 3, ccol = 5;
21)   int c[crow][ccol];
22)  
23)  void main(){
24)   //检查正确性
25)   if (!((acol == brow) && (crow == arow) && (ccol == bcol)))      //正确性检查
26)   return;
27)   //矩阵相乘
28)   for (int i = 0; i< crow; i++){
29)     for (int j = 0; j <ccol; j++){
30)     //求c中的一个元素
31)     c[i][j] = 0;
32)     for (int k = 0; k <acol; k++)
33)     c[i][j] += a[i][k] * b[k][j];
34)    }
35)   }
36)   //输出矩阵乘法的结果
37)   for (int i = 0; i< 3; i++){
38)     for (int j = 0; j < 5; j++)
39)     cout << setw(5) << c[i][j];
40)     cout << endl;
41)   }
42)  
43)  system("pause");
44)  }


```
### 题目23620
问题：程序第5行中， arow的值是否可以修改？

A、  可以修改无数次
B、  可以修改1次
C、  可以修改2次
D、  不可以修改


**解析如下：**

------

解析：
在C++中，`const`关键字用于声明常量，表示该变量的值在声明后不能被修改。因此，`arow`的值在第5行声明为`const int arow = 3;`后，不能被修改。

{'23620':'D'}

------

### 题目23621
问题：在32位计算机中，程序第6行的数组a所占用内存空间大小是?

A、24B
B、36B
C、48B
D、60B


**解析如下：**

------

解析：
在32位计算机中，一个`int`类型占用4个字节。数组`a`是一个3行4列的二维数组，总共有`3 * 4 = 12`个元素。因此，数组`a`所占用的内存空间大小为`12 * 4 = 48`字节。

{'23621':'C'}

------

### 题目23622
问题：程序第6行中，数组a的存放的区域？

A、全局区
B、栈区
C、代码区
D、堆区


**解析如下：**

------

解析：
在C++中，全局变量和静态变量存储在全局区（也称为静态存储区）。数组`a`在第6行被声明为全局变量，因此它存储在全局区。

{'23622':'A'}

------

### 题目23623
问题：程序第6行中定义的数组a中，数组元素的数据类型是？

A、int
B、float
C、double
D、char


**解析如下：**

------

解析：
在程序第6行中，数组`a`被定义为`int a[arow][acol];`，因此数组元素的数据类型是`int`。

{'23623':'A'}

------

### 题目23624
问题：在程序第6行定义的数组a中，a[1][1]代表的是哪一个元素？

A、  4
B、  5
C、  -2
D、  2


**解析如下：**

------

解析：
在程序第6行定义的数组`a`中，`a[1][1]`代表的是第二行第二列的元素。根据数组`a`的初始化值：
```
{ 5, 7, 8, 2 },
{ -2, 4, 1, 1 },
{ 1, 2, 3, 4 }
```
`a[1][1]`对应的元素是`4`。

{'23624':'A'}

------

### 题目23625
问题：程序在main函数执行之前，数组c说法正确的是？

A、  给数组c分配了一个int*类型大小的内存空间
B、  给数组c分配了15个int类型大小的内存空间
C、  给数组c分配了3个int类型大小的内存空间
D、  给数组c分配了5个int类型大小的内存空间


**解析如下：**

------

解析：
在程序第21行中，数组`c`被定义为`int c[crow][ccol];`，其中`crow`为3，`ccol`为5。因此，数组`c`是一个3行5列的二维数组，总共有`3 * 5 = 15`个`int`类型的元素。在`main`函数执行之前，数组`c`已经被分配了15个`int`类型大小的内存空间。

{'23625':'B'}

------

### 题目23626
问题：程序第21行中，数组变量c的存储位置是？

A、  代码区
B、  栈区
C、  全局区
D、  堆区


**解析如下：**

------

解析：
在程序第21行中，数组`c`被定义为全局变量，因此它的存储位置是全局区（也称为静态存储区）。

{'23626':'C'}

------

### 题目23627
问题：程序第21行中，数组c定义后，c的第一行数据是多少？

A、  1, 1, 1, 1, 1
B、  -1, -1, -1, -1, -1
C、  无意义的内存值
D、  0, 0, 0, 0, 0


**解析如下：**

------

解析：
题库提示：数组c是全局数组，未初始化时，系统默认为是整数0。

{'23627':'D'}

------

### 题目23628
问题：程序第28行中，i值为多少时跳出循环？

A、  0
B、  2
C、  3
D、  4


**解析如下：**

------

解析：
在程序第28行中，`for`循环的终止条件是`i < crow`，其中`crow`的值为3。因此，当`i`的值为3时，循环条件`i < crow`不再满足，循环将跳出。

{'23628':'C'}

------

### 题目23629
问题：程序第29行中，若第一层循环变量i=0，当变量j所在的循环完成第2次循环之后，c[i][j]的值是多少？

A、  35
B、  20
C、  66
D、  123


**解析如下：**

------

解析：
j=0时，进行一次循环，c[0][0] = a[0][0] * b[0][0] + a[0][1] * b[1][0] +  a[0][2] * b[2][0] + a[0][3] * b[3][0]
即c[0][0] = 5*4 + 7*4 + 8*2 + 2*1 = 20 + 28 + 16 + 2 = 66，
j=1时，进行二次循环,c[0][1] = a[0][0] * b[1][0] + a[0][1] * b[1][1] +  a[0][2] * b[2][1] + a[0][3] * b[3][1]
即c[0][1] = 5*(-2) + 7*3 + 8*3 + 2*0 = -10+21+24+0=35.

{'23629':'A'}

------

### 题目23630
问题：该程序中，矩阵相乘部分的循环总共执行多少次？

A、  15
B、  60
C、  75
D、  45


**解析如下：**

------

解析：
在程序中，矩阵相乘部分的循环由三层嵌套循环组成。外层循环控制行数，执行`crow`次（3次）；中间层循环控制列数，执行`ccol`次（5次）；内层循环控制矩阵乘法的计算，执行`acol`次（4次）。因此，矩阵相乘部分的循环总共执行的次数为：
```
crow * ccol * acol = 3 * 5 * 4 = 60
```

{'23630':'B'}

------

### 题目23631
问题：程序第一次运行到第37行时，i的值是多少：

A、1
B、2
C、3
D、0


**解析如下：**

------

解析：
在程序第37行中，`for`循环的初始条件是`int i = 0`，因此第一次运行到第37行时，`i`的值是0。

{'23631':'D'}

------

### 题目23632
问题：程序第一次运行到第38行时，j的值是多少：

A、2
B、3
C、4
D、0


**解析如下：**

------

解析：
在程序第38行中，`for`循环的初始条件是`int j = 0`，因此第一次运行到第38行时，`j`的值是0。

{'23632':'D'}

------

### 题目23633
问题：计算结束后，输出的数组c的大小是多少？

A、  3行5列
B、  4行5列
C、  4行4列
D、  3行4列


**解析如下：**

------

解析：
在程序第20行中，数组`c`被定义为`int c[crow][ccol];`，其中`crow`为3，`ccol`为5。因此，数组`c`的大小是3行5列。

{'23633':'A'}

------

### 题目23634
问题：计算结束后，输出数组c时，输出的第10个数字是多少？

A、  1
B、  123
C、  50
D、  -5


**解析如下：**

------

解析：
在计算结束后，数组`c`的大小是3行5列，总共有15个元素。输出的第10个数字对应于数组`c`中的`c[1][4]`。根据矩阵乘法的计算过程，`c[1][4]`的值为：
```
c[1][4] = a[1][0] * b[0][4] + a[1][1] * b[1][4] + a[1][2] * b[2][4] + a[1][3] * b[3][4]
        = -2 * 9 + 4 * 2 + 1 * 7 + 1 * 4
        = -18 + 8 + 7 + 4
        = 1
```
因此，输出的第10个数字是1。

{'23634':'A'}

------

### 题目23635
问题：计算结束后，输出数组c时，输出的第一行数据值是多少？

A、  11, 19, 37, -5, 1
B、  66, 35, 123, 30, 123
C、  22, 13, 58, 19, 50
D、  22, 16, 60, 5, 33


**解析如下：**

------

解析：
在计算结束后，数组`c`的第一行数据值可以通过矩阵乘法的计算过程得出。具体计算如下：
```
c[0][0] = a[0][0] * b[0][0] + a[0][1] * b[1][0] + a[0][2] * b[2][0] + a[0][3] * b[3][0]
        = 5 * 4 + 7 * 4 + 8 * 2 + 2 * 1
        = 20 + 28 + 16 + 2
        = 66

c[0][1] = a[0][0] * b[0][1] + a[0][1] * b[1][1] + a[0][2] * b[2][1] + a[0][3] * b[3][1]
        = 5 * -2 + 7 * 3 + 8 * 3 + 2 * 0
        = -10 + 21 + 24 + 0
        = 35

c[0][2] = a[0][0] * b[0][2] + a[0][1] * b[1][2] + a[0][2] * b[2][2] + a[0][3] * b[3][2]
        = 5 * 3 + 7 * 8 + 8 * 5 + 2 * 6
        = 15 + 56 + 40 + 12
        = 123

c[0][3] = a[0][0] * b[0][3] + a[0][1] * b[1][3] + a[0][2] * b[2][3] + a[0][3] * b[3][3]
        = 5 * 3 + 7 * -1 + 8 * 2 + 2 * 3
        = 15 - 7 + 16 + 6
        = 30

c[0][4] = a[0][0] * b[0][4] + a[0][1] * b[1][4] + a[0][2] * b[2][4] + a[0][3] * b[3][4]
        = 5 * 9 + 7 * 2 + 8 * 7 + 2 * 4
        = 45 + 14 + 56 + 8
        = 123
```
因此，输出的第一行数据值是`66, 35, 123, 30, 123`。

{'23635':'B'}

------

